# КРИТИЧЕСКИЕ ПРАВИЛА ДЛЯ АССИСТЕНТА
# Версия проекта: V3.2.165 (2025.12.07)
# Компания: ООО "НПА Вира-Реалтайм"

## 📅 ОБЯЗАТЕЛЬНОЕ НАЧАЛО КАЖДОГО ОТВЕТА:
**В каждом ответе ПЕРВОЙ строкой писать: "Сегодня дд-мм-гггг, с правилами ознакомлен"**
**Ассистент обязан прочитать до конца и использовать в работе эти правила**

Это дает шанс, что ассистент читает правила перед каждым ответом.

## ⚠️ КРИТИЧЕСКОЕ ПРЕДУПРЕЖДЕНИЕ ПЕРЕД РЕДАКТИРОВАНИЕМ КОДА:

### 🚨 ОБЯЗАТЕЛЬНЫЙ ЧЕКЛИСТ ПЕРЕД ЛЮБЫМ search_replace:

**❌ НИКОГДА НЕ НАЧИНАТЬ редактирование без выполнения ВСЕХ пунктов:**

1. ✅ **ПОЛУЧИТЬ ЯВНОЕ РАЗРЕШЕНИЕ** от пользователя ("да", "+", "согласен", "применяй")
2. ✅ **ПРОЧИТАТЬ** минимум 50 строк контекста через `read_file` (25 строк до + 25 строк после)
3. ✅ **ПРОВЕРИТЬ УНИКАЛЬНОСТЬ** контекста через `grep` - должен встречаться ТОЛЬКО ОДИН РАЗ
4. ✅ **СКОПИРОВАТЬ ТОЧНО** строки из файла, включая все пробелы и отступы
5. ✅ **ПРОВЕРИТЬ СИНТАКСИС** после каждого изменения: `python3 -m py_compile <файл>`
6. ✅ **ПРОВЕРИТЬ ЛИНТЕР** после каждого изменения: `read_lints`

### ❌ ЗАПРЕЩЕНО:
- ❌ Начинать редактирование без явного разрешения пользователя
- ❌ Использовать контекст менее 50 строк
- ❌ Пропускать проверку уникальности через grep
- ❌ Пропускать проверку синтаксиса после изменения
- ❌ Делать несколько изменений одновременно

### 🎯 ПРИНЦИП:
**"50 СТРОК КОНТЕКСТА + РАЗРЕШЕНИЕ + ПРОВЕРКИ = БЕЗОПАСНОЕ РЕДАКТИРОВАНИЕ"**

## 🚨 КРИТИЧЕСКОЕ ПРАВИЛО: ВСЕГДА ЖДАТЬ СОГЛАСОВАНИЯ ИЗМЕНЕНИЙ

### ❌ ИЗМЕНЕНИЯ КОДА БЕЗ СОГЛАСОВАНИЯ ЗАПРЕЩЕНЫ:
- Изменять код проекта без явного разрешения пользователя запрещено
- Всегда ждать согласования предложенных изменений перед их применением
- Всегда сначала предлагать план изменений и объяснять что будет сделано
- Применять изменения только после получения явного разрешения ("да", "+", "согласен", "применяй")

### ✅ ПРАВИЛЬНЫЙ АЛГОРИТМ РАБОТЫ:
1. **ШАГ 1:** Анализ проблемы
2. **ШАГ 2:** Предложение плана решения с объяснением изменений
3. **ШАГ 3:** ОЖИДАНИЕ разрешения от пользователя
4. **ШАГ 4:** ТОЛЬКО после разрешения - применение изменений

### 🎯 ПРИНЦИП:
**"БЕЗ РАЗРЕШЕНИЯ - НИКАКИХ ИЗМЕНЕНИЙ!"**
Ассистент ВСЕГДА ждет согласования перед любыми изменениями кода!

## 🚨 КРИТИЧЕСКОЕ ПРАВИЛО БЕЗОПАСНОСТИ ПРОЕКТА:

### 📁 РАБОТА ТОЛЬКО В ПРЕДЕЛАХ ТЕКУЩЕГО ПРОЕКТА:
- ✅ **РАБОТАТЬ ТОЛЬКО** в папке `FSA-AstraInstall/`
- ❌ **ЗАПРЕЩЕНО** заходить в корневую папку `/Volumes/FSA-PRJ/Project/`
- ❌ **ЗАПРЕЩЕНО** работать за пределами текущего проекта (только внутри `FSA-AstraInstall/`)
- ❌ **ЗАПРЕЩЕНО** использовать абсолютные пути за пределами `FSA-AstraInstall/`
- ✅ **ВСЕГДА** использовать относительные пути от корня проекта `FSA-AstraInstall/`
- ❌ **ЗАПРЕЩЕНО** в коде данного проекта использовать символы эмодзи

### 🔒 ПРИНЦИП ИЗОЛЯЦИИ:
**"ОДИН ПРОЕКТ = ОДНА ПАПКА"** - ассистент работает исключительно в рамках текущего проекта!

### ⚠️ КОНТРОЛЬ ДОСТУПА:
- Все команды выполняются только в `FSA-AstraInstall/`
- Все файловые операции ограничены папкой проекта
- Git операции только в контексте текущего проекта
- Никаких переходов в родительские или соседние директории

## 🔧 ПРАВИЛА ИСПОЛЬЗОВАНИЯ ИНСТРУМЕНТОВ:

### 📝 СОЗДАНИЕ НОВЫХ ФАЙЛОВ:
- ✅ **ВСЕГДА** использовать команду `cat` с heredoc для создания файлов: `cat > filename << 'EOF'`
- ❌ **ЗАПРЕЩЕНО** использовать `write` tool (создает пустые файлы)
- ❌ **ЗАПРЕЩЕНО** использовать `echo` или `printf` для создания многострочных файлов
- ❌ **КРИТИЧЕСКИ ЗАПРЕЩЕНО:** Использовать Python-скрипты (`fix_*.py`) для редактирования кода - только `search_replace`!
- ✅ Всегда проверять размер файла: `ls -la filename`
- ✅ Всегда проверять содержимое: `cat filename`

### ✏️ РЕДАКТИРОВАНИЕ СУЩЕСТВУЮЩИХ ФАЙЛОВ:
- ✅ **РАЗРЕШЕНО использовать `search_replace` для редактирования кода** при строгом соблюдении правил безопасности (см. раздел "🚨 БЕЗОПАСНОЕ ИСПОЛЬЗОВАНИЕ search_replace")
- ❌ **ЗАПРЕЩЕНО использовать `MultiEdit`** (слишком рискованно для больших файлов)
- ❌ **ЗАПРЕЩЕНО использовать `sed` для редактирования кода** — создаёт конфликты с IDE
- ✅ **ИСКЛЮЧЕНИЕ:** `sed` разрешён ТОЛЬКО при создании снимков (коммитов) по правилам из раздела "📁 ПРАВИЛО ДЛЯ ИСТОРИИ КОММИТОВ" для массового обновления версий/дат во всех файлах проекта
- ⚠️ **ВАЖНО:** После использования `sed` в процессе создания снимка обязательно перечитать файл через `read_file` перед дальнейшими изменениями, если они потребуются

## 🚨 КРИТИЧЕСКОЕ ПРАВИЛО: ИСПОЛЬЗОВАНИЕ УНИВЕРСАЛЬНОГО PRINT

### ✅ ОБЯЗАТЕЛЬНОЕ ИСПОЛЬЗОВАНИЕ УНИВЕРСАЛЬНОГО PRINT:
- ✅ **ВСЕГДА** использовать стандартный `print()` (который автоматически вызывает `universal_print`)
- ✅ **ВСЕГДА** использовать параметр `level` для указания уровня сообщения
- ✅ **НИКОГДА** не использовать префиксы в тексте сообщений (`[DEBUG]`, `[ERROR]`, `[WARNING]`, `[INFO]`, `ОШИБКА:`, `ВНИМАНИЕ:`)
- ✅ **ВСЕГДА** использовать параметры вместо префиксов в тексте

### 📋 ПРАВИЛА ВЫБОРА УРОВНЯ СООБЩЕНИЯ:

#### 🔍 DEBUG (Отладочные сообщения):
- **Использовать для:** Детальная отладочная информация о работе функций, методов, переменных
- **Примеры:** Вызовы функций, значения переменных, промежуточные состояния
- **Формат:** `print(f"Текст сообщения", level='DEBUG')`
- **Пример:** `print(f"WinetricksHandler.install() вызван с component_id={component_id}", level='DEBUG')`

#### ℹ️ INFO (Информационные сообщения):
- **Использовать для:** Обычная информация о ходе выполнения программы
- **Примеры:** Начало/завершение операций, успешные действия, общая информация
- **Формат:** `print("Текст сообщения")` или `print("Текст сообщения", level='INFO')`
- **Пример:** `print("Установка компонента начата")`

#### ⚠️ WARNING (Предупреждения):
- **Использовать для:** Предупреждения о потенциальных проблемах, не критичных ошибках
- **Примеры:** Недостаточно ресурсов, пропуск операций, нестандартные ситуации
- **Формат:** `print("Текст сообщения", level='WARNING')`
- **Пример:** `print("Не все процессы остановлены, продолжаем", level='WARNING')`

#### ❌ ERROR (Ошибки):
- **Использовать для:** Критичные ошибки, сбои операций, проблемы требующие внимания
- **Примеры:** Ошибки установки, сбои проверок, критические проблемы
- **Формат:** `print("Текст сообщения", level='ERROR')`
- **Пример:** `print("Не удалось установить компонент", level='ERROR')`

### 🔧 ДОПОЛНИТЕЛЬНЫЕ ПАРАМЕТРЫ:

#### 📊 stream (Поток логирования):
- `stream='analysis'` (по умолчанию) - анализ-поток для обработанных сообщений
- `stream='raw'` - RAW-поток для необработанного вывода процессов (apt, winetricks и т.д.)
- **Пример:** `print("Вывод процесса", stream='raw')`

#### 🖥️ gui_log (Отображение в GUI):
- `gui_log=True` - отображать сообщение в GUI логе
- `gui_log=False` (по умолчанию) - не отображать в GUI
- **Пример:** `print("Сообщение для GUI", gui_log=True)`

### ❌ ЗАПРЕЩЕНО:

#### 🚫 ПРЕФИКСЫ В ТЕКСТЕ СООБЩЕНИЙ:
- ❌ **ЗАПРЕЩЕНО:** `print(f"[DEBUG] Текст сообщения")`
- ❌ **ЗАПРЕЩЕНО:** `print(f"[ERROR] Текст сообщения")`
- ❌ **ЗАПРЕЩЕНО:** `print(f"[WARNING] Текст сообщения")`
- ❌ **ЗАПРЕЩЕНО:** `print(f"[INFO] Текст сообщения")`
- ❌ **ЗАПРЕЩЕНО:** `print("ОШИБКА: Текст сообщения")`
- ❌ **ЗАПРЕЩЕНО:** `print("ВНИМАНИЕ: Текст сообщения")`
- ❌ **ЗАПРЕЩЕНО:** `print("КРИТИЧЕСКАЯ ОШИБКА: Текст сообщения")`

#### ✅ ПРАВИЛЬНО:
- ✅ **ПРАВИЛЬНО:** `print(f"Текст сообщения", level='DEBUG')`
- ✅ **ПРАВИЛЬНО:** `print(f"Текст сообщения", level='ERROR')`
- ✅ **ПРАВИЛЬНО:** `print(f"Текст сообщения", level='WARNING')`
- ✅ **ПРАВИЛЬНО:** `print(f"Текст сообщения", level='INFO')`

### 📝 ПРИМЕРЫ ПРАВИЛЬНОГО ИСПОЛЬЗОВАНИЯ:

#### ✅ ПРАВИЛЬНО:
```python
# Отладочное сообщение
print(f"WinetricksHandler.install() вызван с component_id={component_id}", level='DEBUG')

# Информационное сообщение
print("Установка компонента начата")

# Предупреждение
print("Не все процессы остановлены, продолжаем", level='WARNING')

# Ошибка
print("Не удалось установить компонент", level='ERROR')

# Сообщение для GUI
print("Операция завершена", gui_log=True)

# RAW-поток для вывода процесса
print("Вывод apt-get", stream='raw')
```

#### ❌ НЕПРАВИЛЬНО:
```python
# НЕПРАВИЛЬНО: префикс [DEBUG] в тексте
print(f"[DEBUG] WinetricksHandler.install() вызван с component_id={component_id}")

# НЕПРАВИЛЬНО: префикс [ERROR] в тексте
print(f"[ERROR] Не удалось установить компонент")

# НЕПРАВИЛЬНО: префикс ОШИБКА: в тексте
print("ОШИБКА: Не удалось установить компонент")

# НЕПРАВИЛЬНО: префикс ВНИМАНИЕ: в тексте
print("ВНИМАНИЕ: Не все процессы остановлены")
```

### 🎯 ПРИНЦИП:
**"ТОЛЬКО ПАРАМЕТРЫ, НИКАКИХ ПРЕФИКСОВ В ТЕКСТЕ!"**
Все сообщения должны использовать параметр `level` вместо префиксов в тексте. Универсальный print автоматически добавляет правильные префиксы и метки времени.

### ⚠️ ВАЖНО:
- Универсальный print автоматически добавляет префикс уровня (`[DEBUG]`, `[ERROR]`, `[WARNING]`, `[INFO]`) и метку времени
- Все сообщения логируются через DualStreamLogger с автоматическими метками времени
- Префиксы в тексте сообщений дублируют функциональность и нарушают единообразие логирования

## 🚨 ПРАВИЛА ИЗМЕНЕНИЯ КОДА:

### ❌ ЗАПРЕЩЕНО БЕЗ РАЗРЕШЕНИЯ:
- Создавать новые файлы
- Удалять файлы
- Делать коммиты
- Любые изменения структуры проекта
- **ВОССТАНАВЛИВАТЬ ФАЙЛЫ ИЗ GIT** (`git checkout`, `git restore`)

### ✅ РЕДАКТИРОВАНИЕ СУЩЕСТВУЮЩИХ ФАЙЛОВ:
- Можно делать напрямую через инструменты редактирования
- БЕЗ предварительных запросов
- Пользователь контролирует через редактор

### 📋 АЛГОРИТМ ДЛЯ НОВЫХ ФАЙЛОВ/КОММИТОВ:
1. Пользователь просит создать файл/коммит
2. Анализирую задачу
3. Предлагаю план
4. ЖДУ разрешения пользователя
5. Выполняю ТОЛЬКО после разрешения

## 🚨 КРИТИЧЕСКОЕ ПРАВИЛО ДЛЯ ИЗМЕНЕНИЙ КОДА:

### 📝 ПОШАГОВЫЕ ИЗМЕНЕНИЯ:
- ✅ **ВСЕГДА** вносить изменения ПОШАГОВО
- ✅ **ПОСЛЕ КАЖДОГО ШАГА** проверять синтаксис: `python3 -m py_compile filename`
- ✅ **ЖДАТЬ ПОДТВЕРЖДЕНИЯ** сохранения от пользователя
- ✅ **ТОЛЬКО ПОСЛЕ ПОДТВЕРЖДЕНИЯ** запускать тесты
- ❌ **НИКОГДА** не запускать код без подтверждения сохранения
- ❌ **НИКОГДА** не делать несколько изменений одновременно

### 🔄 АЛГОРИТМ ИЗМЕНЕНИЙ:
1. **ШАГ 1:** Внести одно изменение
2. **ПРОВЕРКА:** `python3 -m py_compile filename`
3. **ОЖИДАНИЕ:** Подтверждения сохранения от пользователя
4. **ШАГ 2:** Следующее изменение (только после подтверждения)
5. **ПОВТОР:** До завершения всех изменений
6. **ТЕСТ:** Только после подтверждения всех изменений

## 🚨 ПРАВИЛО ИЗБЕЖАНИЯ КАСКАДНЫХ ОШИБОК ОТСТУПОВ:

### ❌ ПРОБЛЕМЫ ПРИ ИЗМЕНЕНИИ КОДА:
- **КАСКАДНЫЕ ОШИБКИ:** Удаление одной строки ломает отступы по всему файлу
- **DOMINO EFFECT:** Ошибки отступов "размножаются" по всему коду
- **НЕДОСТАТОЧНЫЙ КОНТЕКСТ:** Слишком маленькие фрагменты для замены
- **МАССОВЫЕ ИЗМЕНЕНИЯ:** Попытки изменить много строк сразу

### ✅ ПРАВИЛЬНЫЙ ПОДХОД ДЛЯ ИЗМЕНЕНИЙ:
1. **ЧИТАТЬ МАКСИМАЛЬНЫЙ КОНТЕКСТ** - минимум 50 строк вокруг изменения
2. **АНАЛИЗИРОВАТЬ СТРУКТУРУ** - понимать блоки кода, отступы, скобки
3. **ПРОВЕРЯТЬ ГРАНИЦЫ БЛОКОВ** - где начинается и заканчивается функция/класс
4. **ДЕЛАТЬ ПО ОДНОМУ ИЗМЕНЕНИЮ** - не массово
5. **ПРОВЕРЯТЬ ПОСЛЕ КАЖДОГО** - синтаксис и линтер
6. **ИСПОЛЬЗОВАТЬ ТОЧНЫЕ СТРОКИ** - с максимальным контекстом для замены

### 🔄 НОВЫЙ АЛГОРИТМ ДЛЯ СЛОЖНЫХ ИЗМЕНЕНИЙ:
1. **АНАЛИЗ:** Найти точные строки для изменения
2. **КОНТЕКСТ:** Прочитать минимум 50 строк вокруг
3. **СТРУКТУРА:** Понять границы блоков кода
4. **ЗАМЕНА:** Использовать максимальный контекст для точной замены
5. **ПРОВЕРКА:** Проверить синтаксис и линтер
6. **ПОДТВЕРЖДЕНИЕ:** Убедиться что нет ошибок

### 🎯 ПРИНЦИП:
**"МАКСИМАЛЬНЫЙ КОНТЕКСТ + ТОЧНАЯ ЗАМЕНА = БЕЗ ОШИБОК"**
Ассистент делает изменения, но с максимальной осторожностью!

## 🚨 КРИТИЧЕСКОЕ ПРАВИЛО: ПРОВЕРКА СИНТАКСИСА ПОСЛЕ КАЖДОГО ИЗМЕНЕНИЯ:

### ✅ ОБЯЗАТЕЛЬНЫЕ ПРАВИЛА ПРИ РЕДАКТИРОВАНИИ КОДА PYTHON:

1. **ПРОВЕРКА СИНТАКСИСА ПОСЛЕ КАЖДОГО ИЗМЕНЕНИЯ:**
   - ✅ **ВСЕГДА** после использования `search_replace` проверять синтаксис: `python3 -m py_compile <файл>`
   - ✅ Если есть ошибки синтаксиса - исправлять их **СРАЗУ**, не накапливать
   - ✅ **ЗАПРЕЩЕНО** продолжать редактирование, если предыдущее изменение сломало синтаксис

2. **ТОЧНОЕ СОВПАДЕНИЕ В search_replace:**
   - ✅ Использовать ТОЧНОЕ совпадение строк из файла в `search_replace`
   - ✅ Включать достаточный контекст для уникальной идентификации места замены
   - ✅ Копировать строки ТОЧНО из файла, не добавляя лишних пробелов

3. **ТОЧЕЧНЫЕ ИЗМЕНЕНИЯ:**
   - ✅ Вносить изменения **ПО ОДНОМУ МЕСТУ** за раз через `search_replace`
   - ❌ **ЗАПРЕЩЕНО** применять массовые автоматические исправления отступов
   - ✅ Если нужно исправить несколько мест - делать это **ПОСЛЕДОВАТЕЛЬНО** через отдельные `search_replace` с проверкой после каждого

4. **ПРОВЕРКА ОТСТУПОВ ПЕРЕД ЗАМЕНОЙ:**
   - ✅ Перед заменой проверять отступы в исходном коде
   - ✅ Сохранять существующую структуру отступов
   - ❌ **ЗАПРЕЩЕНО** менять отступы, если они не связаны с текущим изменением

5. **ЧТЕНИЕ КОНТЕКСТА ПЕРЕД ИЗМЕНЕНИЕМ:**
   - ✅ Читать достаточный контекст (минимум 50 строк до и после) перед изменением
   - ✅ Убеждаться, что понимаю структуру кода и уровень вложенности

6. **ОТКАТ ПРИ ОШИБКАХ:**
   - ✅ Если после изменения появились множественные ошибки синтаксиса - **ОТКАТИТЬ** изменение и переделать более аккуратно
   - ❌ **ЗАПРЕЩЕНО** пытаться исправлять ошибки массово, если они появились из-за моего изменения

### 🎯 ПРИНЦИП:
**"ОДНО ИЗМЕНЕНИЕ → ПРОВЕРКА СИНТАКСИСА → СЛЕДУЮЩЕЕ ИЗМЕНЕНИЕ"**
Ассистент делает изменения точечно и проверяет синтаксис после каждого шага!

## 🚨 КРИТИЧЕСКОЕ ПРАВИЛО: БЕЗОПАСНОЕ ИСПОЛЬЗОВАНИЕ search_replace

### ✅ ОБЯЗАТЕЛЬНЫЕ ПРАВИЛА БЕЗОПАСНОСТИ:

**ПЕРЕД ИСПОЛЬЗОВАНИЕМ search_replace ОБЯЗАТЕЛЬНО:**

1. **ШАГ 1: ЧТЕНИЕ ТОЧНОГО КОНТЕКСТА**
   - ✅ Прочитать точные строки через `read_file` с достаточным контекстом (минимум 50 строк до и после)
   - ✅ Скопировать строки ТОЧНО из файла, включая все пробелы и отступы
   - ✅ НЕ добавлять лишних пробелов или изменять отступы

2. **ШАГ 2: ПРОВЕРКА УНИКАЛЬНОСТИ КОНТЕКСТА**
   - ✅ Использовать `grep` для проверки, что контекст встречается в файле ТОЛЬКО ОДИН РАЗ
   - ✅ Команда: `grep -n "уникальная_строка" файл.py` должна вернуть ТОЛЬКО одну строку
   - ❌ **ЗАПРЕЩЕНО** использовать контекст, который встречается более одного раза

3. **ШАГ 3: ФОРМИРОВАНИЕ БЕЗОПАСНОГО КОНТЕКСТА**
   - ✅ Включать достаточно строк для однозначной идентификации (минимум 50 строк уникального кода)
   - ✅ Включать уникальные строки: имена функций, классов, комментарии, специфичные значения
   - ❌ **ЗАПРЕЩЕНО** использовать только общие строки (например, только `return`, `if`, `def`)

4. **ШАГ 4: ПРИМЕНЕНИЕ ЗАМЕНЫ**
   - ✅ Использовать `search_replace` с точным контекстом
   - ✅ Сохранять все отступы в незатронутых частях кода
   - ✅ Изменять ТОЛЬКО необходимые строки

5. **ШАГ 5: ПРОВЕРКА ПОСЛЕ ЗАМЕНЫ**
   - ✅ **ОБЯЗАТЕЛЬНО** проверить синтаксис: `python3 -m py_compile <файл>`
   - ✅ **ОБЯЗАТЕЛЬНО** проверить линтер: `read_lints`
   - ✅ Если есть ошибки - ОТКАТИТЬ изменение и переделать с более точным контекстом

### ❌ ЗАПРЕЩЕНО:

- ❌ Использовать `search_replace` без предварительной проверки уникальности контекста через `grep`
- ❌ Использовать слишком маленький контекст (менее 50 строк уникального кода) - недостаточно для однозначной идентификации
- ❌ Пропускать проверки синтаксиса и линтера после замены
- ❌ Менять отступы в незатронутых частях кода

### ⚠️ РЕКОМЕНДАЦИИ:

- ⚠️ Для больших блоков кода (более 20-30 строк) рекомендуется разбить на несколько замен для снижения риска ошибок
- ⚠️ Размер контекста должен быть достаточным для однозначной идентификации места замены (минимум 50 строк уникального кода)

## 🚨 КРИТИЧЕСКОЕ ПРАВИЛО: СТРУКТУРА КЛАССОВ И ЗАГОЛОВКИ

### ✅ ПРАВИЛЬНОЕ РАСПОЛОЖЕНИЕ ЗАГОЛОВКОВ КЛАССОВ:

**ЗАГОЛОВКИ КЛАССОВ ДОЛЖНЫ БЫТЬ ВНУТРИ КЛАССА:**

```python
# ❌ НЕПРАВИЛЬНО - заголовок ПЕРЕД классом:
# ============================================================================
# КЛАСС МОНИТОРИНГА ИЗМЕНЕНИЙ В ДИРЕКТОРИИ
# ============================================================================
class DirectoryMonitor(object):
    """Класс для мониторинга изменений в директории"""
    ...

# ✅ ПРАВИЛЬНО - заголовок ВНУТРИ класса, сразу после определения:
class DirectoryMonitor(object):
# ============================================================================
# КЛАСС МОНИТОРИНГА ИЗМЕНЕНИЙ В ДИРЕКТОРИИ
# ============================================================================
    """Класс для мониторинга изменений в директории"""
    ...
```

### 📋 ОБЯЗАТЕЛЬНЫЕ ПРАВИЛА:

1. **РАСПОЛОЖЕНИЕ ЗАГОЛОВКА:**
   - ✅ Заголовок класса ДОЛЖЕН находиться ВНУТРИ класса
   - ✅ Заголовок ДОЛЖЕН быть сразу после строки определения класса (`class ClassName:`)
   - ✅ Заголовок ДОЛЖЕН быть перед docstring класса
   - ❌ **ЗАПРЕЩЕНО** размещать заголовок ПЕРЕД определением класса

2. **ПРИ СОЗДАНИИ НОВЫХ КЛАССОВ:**
   - ✅ Всегда размещать заголовок внутри класса, сразу после `class ClassName:`
   - ✅ Формат: `# ============================================================================` с описанием класса

3. **ПРИ РЕДАКТИРОВАНИИ СУЩЕСТВУЮЩИХ КЛАССОВ:**
   - ✅ Проверять расположение заголовка при любых изменениях класса
   - ✅ Если заголовок находится ПЕРЕД классом - переместить его ВНУТРЬ класса
   - ✅ Использовать `search_replace` для перемещения заголовка внутрь класса

4. **ПРОВЕРКА ПЕРЕД КОММИТОМ:**
   - ✅ Проверять, что все заголовки классов находятся внутри классов
   - ✅ Использовать `grep` для поиска заголовков перед классами: `grep -B 2 "^class " файл.py`

### 🎯 ПРИНЦИП:
**"ЗАГОЛОВОК КЛАССА = ВНУТРИ КЛАССА, СРАЗУ ПОСЛЕ ОПРЕДЕЛЕНИЯ"**
Ассистент всегда размещает заголовки классов внутри класса, а не перед ним!

## 🚨 КРИТИЧЕСКОЕ ПРАВИЛО: АЛГОРИТМЫ ОПЕРАЦИЙ С КОДОМ

### 📋 ОБЩИЕ ПРИНЦИПЫ ДЛЯ ВСЕХ ОПЕРАЦИЙ:

**ОБЯЗАТЕЛЬНЫЕ ШАГИ ПЕРЕД КАЖДЫМ search_replace:**
1. ✅ **ОБЯЗАТЕЛЬНО** прочитать точный контекст через `read_file` (минимум 50 строк до и после)
2. ✅ **ОБЯЗАТЕЛЬНО** проверить уникальность контекста через `grep`: `grep -n "уникальная_строка" файл.py` должна вернуть ТОЛЬКО одну строку
3. ❌ **ЗАПРЕЩЕНО** применять `search_replace` без предварительной проверки уникальности контекста

**ОБЯЗАТЕЛЬНЫЕ ШАГИ ПОСЛЕ КАЖДОГО search_replace:**
1. ✅ **ОБЯЗАТЕЛЬНО** проверить синтаксис: `python3 -m py_compile <файл>`
2. ✅ **ОБЯЗАТЕЛЬНО** проверить линтер: `read_lints` для измененного файла
3. ✅ **ОБЯЗАТЕЛЬНО** проверить фактический результат: `grep -n "уникальный_идентификатор" файл.py` или `read_file` для подтверждения изменений
4. ✅ Только после успешных проверок - переход к следующему шагу
5. ❌ **ЗАПРЕЩЕНО** пропускать любую из проверок
6. ❌ **ЗАПРЕЩЕНО** продолжать после ошибок синтаксиса или линтера

**ОБЯЗАТЕЛЬНЫЕ ПРАВИЛА:**
- ✅ Использовать **ТОЛЬКО `search_replace`** для всех операций с кодом
- ✅ Включать достаточный контекст (минимум 50 строк уникального кода)
- ✅ Сохранять все отступы в незатронутых частях кода
- ✅ Делать ОДНО изменение за раз (один `search_replace` → все проверки → следующий `search_replace`)
- ❌ **ЗАПРЕЩЕНО** делать несколько операций одновременно
- ❌ **КРИТИЧЕСКИ ЗАПРЕЩЕНО** использовать Python-скрипты (`fix_*.py`) для редактирования кода - изменения не видны в IDE и нельзя отменить!
- ❌ **КРИТИЧЕСКИ ЗАПРЕЩЕНО** использовать любые скрипты для редактирования кода - только `search_replace`!

### 🔧 АЛГОРИТМ 1: ДОБАВЛЕНИЕ МЕТОДА В КЛАСС

**ЦЕЛЬ:** Добавить новый метод в конец класса или в определенное место внутри класса.

**ШАГИ:**

1. **ШАГ 1: ОПРЕДЕЛЕНИЕ МЕСТА ВСТАВКИ**
   - ✅ Прочитать конец класса через `read_file` (минимум 50 строк)
   - ✅ Найти последний метод класса или место, куда нужно вставить
   - ✅ Определить правильные отступы для нового метода

2. **ШАГ 2: ПРОВЕРКА УНИКАЛЬНОСТИ КОНТЕКСТА**
   - ✅ Использовать `grep` для проверки, что контекст вставки уникален
   - ✅ Команда: `grep -n "последняя_строка_перед_вставкой" файл.py` должна вернуть ТОЛЬКО одну строку
   - ❌ **ЗАПРЕЩЕНО** использовать контекст, который встречается более одного раза

3. **ШАГ 3: ФОРМИРОВАНИЕ КОНТЕКСТА ДЛЯ ВСТАВКИ**
   - ✅ Включить минимум 50 строк перед местом вставки
   - ✅ Включить уникальные строки: имена методов, комментарии, специфичные значения
   - ✅ Скопировать строки ТОЧНО из файла, включая все пробелы и отступы

4. **ШАГ 4: ПРИМЕНЕНИЕ ВСТАВКИ**
   - ✅ Использовать `search_replace` с точным контекстом
   - ✅ Вставить новый метод с правильными отступами
   - ✅ Формат: заменить контекст на "контекст + новый метод"

5. **ШАГ 5: ПРОВЕРКА РЕЗУЛЬТАТА**
   - ✅ Проверить синтаксис: `python3 -m py_compile <файл>`
   - ✅ Проверить линтер: `read_lints`
   - ✅ Проверить фактическое расположение: `grep -n "def новый_метод" файл.py`
   - ✅ Убедиться, что метод находится в правильном месте класса

**ПРИМЕР:**
```python
# Контекст для вставки (последний метод класса):
    def last_method(self):
        return "result"

# Вставляем новый метод:
    def last_method(self):
        return "result"
    
    def new_method(self):
        """Новый метод"""
        return "new_result"
```

### 🔧 АЛГОРИТМ 2: РЕДАКТИРОВАНИЕ СУЩЕСТВУЮЩЕГО МЕТОДА

**ЦЕЛЬ:** Изменить содержимое существующего метода, сохранив его расположение.

**ШАГИ:**

1. **ШАГ 1: ЧТЕНИЕ ТОЧНОГО КОНТЕКСТА МЕТОДА**
   - ✅ Прочитать метод через `read_file` с достаточным контекстом (минимум 50 строк до и после)
   - ✅ Определить начало и конец метода
   - ✅ Скопировать метод ТОЧНО из файла, включая все пробелы и отступы

2. **ШАГ 2: ПРОВЕРКА УНИКАЛЬНОСТИ КОНТЕКСТА**
   - ✅ Использовать `grep` для проверки, что контекст метода уникален
   - ✅ Команда: `grep -n "def имя_метода" файл.py` должна вернуть ТОЛЬКО одну строку
   - ✅ Если метод встречается несколько раз - добавить больше контекста (предыдущий/следующий метод)

3. **ШАГ 3: ФОРМИРОВАНИЕ БЕЗОПАСНОГО КОНТЕКСТА**
   - ✅ Включить минимум 50 строк уникального кода до и после метода
   - ✅ Включить уникальные строки: имена соседних методов, комментарии, специфичные значения
   - ❌ **ЗАПРЕЩЕНО** использовать только общие строки (например, только `return`, `if`, `def`)

4. **ШАГ 4: ПРИМЕНЕНИЕ ИЗМЕНЕНИЙ**
   - ✅ Использовать `search_replace` с точным контекстом
   - ✅ Заменить старый метод на новый с теми же отступами
   - ✅ Сохранить все отступы в незатронутых частях кода

5. **ШАГ 5: ПРОВЕРКА РЕЗУЛЬТАТА**
   - ✅ Проверить синтаксис: `python3 -m py_compile <файл>`
   - ✅ Проверить линтер: `read_lints`
   - ✅ Проверить фактическое содержимое: `read_file` для измененного метода
   - ✅ Убедиться, что метод находится в том же месте

### 🔧 АЛГОРИТМ 3: УДАЛЕНИЕ МЕТОДА ИЗ КЛАССА

**ЦЕЛЬ:** Удалить метод из класса, сохранив структуру остального кода.

**ШАГИ:**

1. **ШАГ 1: ЧТЕНИЕ ТОЧНОГО КОНТЕКСТА МЕТОДА**
   - ✅ Прочитать метод через `read_file` с достаточным контекстом (минимум 50 строк до и после)
   - ✅ Определить начало и конец метода
   - ✅ Включить предыдущий и следующий методы для уникальности

2. **ШАГ 2: ПРОВЕРКА УНИКАЛЬНОСТИ КОНТЕКСТА**
   - ✅ Использовать `grep` для проверки, что контекст метода уникален
   - ✅ Команда: `grep -n "def удаляемый_метод" файл.py` должна вернуть ТОЛЬКО одну строку
   - ✅ Если метод встречается несколько раз - добавить больше контекста (предыдущий/следующий метод, имя класса)

3. **ШАГ 3: ФОРМИРОВАНИЕ БЕЗОПАСНОГО КОНТЕКСТА**
   - ✅ Включить минимум 50 строк уникального кода до и после метода
   - ✅ Включить конец предыдущего метода и начало следующего метода
   - ✅ Включить уникальные строки: имена соседних методов, комментарии

4. **ШАГ 4: ПРИМЕНЕНИЕ УДАЛЕНИЯ**
   - ✅ Использовать `search_replace` с точным контекстом
   - ✅ Заменить "предыдущий_метод + удаляемый_метод + следующий_метод" на "предыдущий_метод + следующий_метод"
   - ✅ Сохранить все отступы в незатронутых частях кода

5. **ШАГ 5: ПРОВЕРКА РЕЗУЛЬТАТА**
   - ✅ Проверить синтаксис: `python3 -m py_compile <файл>`
   - ✅ Проверить линтер: `read_lints`
   - ✅ Проверить, что метод удален: `grep -n "def удаляемый_метод" файл.py` должно вернуть пустой результат
   - ✅ Проверить, что соседние методы не повреждены: `read_file` для области удаления

### 🔧 АЛГОРИТМ 4: КОПИРОВАНИЕ МЕТОДОВ МЕЖДУ КЛАССАМИ

**ЦЕЛЬ:** Скопировать один или несколько методов из одного класса в другой.

**ШАГИ:**

1. **ШАГ 1: ЧТЕНИЕ ИСХОДНОГО МЕТОДА**
   - ✅ Прочитать метод из исходного класса через `read_file` с достаточным контекстом
   - ✅ Определить начало и конец метода
   - ✅ Скопировать метод ТОЧНО, включая все пробелы и отступы

2. **ШАГ 2: ОПРЕДЕЛЕНИЕ МЕСТА ВСТАВКИ В ЦЕЛЕВОМ КЛАССЕ**
   - ✅ Прочитать конец целевого класса через `read_file` (минимум 50 строк)
   - ✅ Найти последний метод класса или место, куда нужно вставить
   - ✅ Определить правильные отступы для нового метода

3. **ШАГ 3: ПРОВЕРКА УНИКАЛЬНОСТИ КОНТЕКСТА**
   - ✅ Использовать `grep` для проверки, что контекст вставки в целевом классе уникален
   - ✅ Команда: `grep -n "последняя_строка_перед_вставкой" файл.py` должна вернуть ТОЛЬКО одну строку

4. **ШАГ 4: ПРИМЕНЕНИЕ ВСТАВКИ**
   - ✅ Использовать `search_replace` с точным контекстом целевого класса
   - ✅ Вставить скопированный метод с правильными отступами
   - ✅ Адаптировать отступы метода под структуру целевого класса

5. **ШАГ 5: ПРОВЕРКА РЕЗУЛЬТАТА**
   - ✅ Проверить синтаксис: `python3 -m py_compile <файл>`
   - ✅ Проверить линтер: `read_lints`
   - ✅ Проверить фактическое расположение: `grep -n "def скопированный_метод" файл.py`
   - ✅ Убедиться, что метод находится в правильном классе

**ПРИМЕЧАНИЕ:** Если нужно скопировать несколько методов - выполнять по одному методу за раз с проверками после каждого.

### 🔧 АЛГОРИТМ 5: ДОБАВЛЕНИЕ ПЕРЕМЕННЫХ В __init__ МЕТОД

**ЦЕЛЬ:** Добавить новые переменные в метод `__init__` класса.

**ШАГИ:**

1. **ШАГ 1: ЧТЕНИЕ ТОЧНОГО КОНТЕКСТА __init__**
   - ✅ Прочитать метод `__init__` через `read_file` с достаточным контекстом
   - ✅ Определить место, куда нужно добавить переменные (обычно в конец блока инициализации)
   - ✅ Скопировать метод ТОЧНО, включая все пробелы и отступы

2. **ШАГ 2: ПРОВЕРКА УНИКАЛЬНОСТИ КОНТЕКСТА**
   - ✅ Использовать `grep` для проверки, что контекст `__init__` уникален
   - ✅ Команда: `grep -n "def __init__" файл.py` должна вернуть ТОЛЬКО одну строку
   - ✅ Если классов несколько - добавить больше контекста (имя класса, предыдущие строки)

3. **ШАГ 3: ФОРМИРОВАНИЕ БЕЗОПАСНОГО КОНТЕКСТА**
   - ✅ Включить минимум 50 строк уникального кода из `__init__`
   - ✅ Включить последние строки перед местом вставки
   - ✅ Включить уникальные строки: имена существующих переменных, комментарии

4. **ШАГ 4: ПРИМЕНЕНИЕ ДОБАВЛЕНИЯ**
   - ✅ Использовать `search_replace` с точным контекстом
   - ✅ Добавить новые переменные с правильными отступами
   - ✅ Сохранить все отступы в незатронутых частях кода

5. **ШАГ 5: ПРОВЕРКА РЕЗУЛЬТАТА**
   - ✅ Проверить синтаксис: `python3 -m py_compile <файл>`
   - ✅ Проверить линтер: `read_lints`
   - ✅ Проверить фактическое содержимое: `read_file` для измененного `__init__`
   - ✅ Убедиться, что новые переменные добавлены правильно

### 🔧 АЛГОРИТМ 6: ПЕРЕМЕЩЕНИЕ КЛАССА ИЛИ БОЛЬШОГО БЛОКА КОДА

**ЦЕЛЬ:** Переместить класс или большой блок кода из одного места в другое.

**🚨 КРИТИЧЕСКИ ВАЖНЫЙ ПРИНЦИП: ПОРЯДОК ОПЕРАЦИЙ**
- ✅ **ВСЕГДА** сначала КОПИРОВАТЬ (вставлять) блок в новое место
- ✅ **ТОЛЬКО ПОСЛЕ** успешной вставки и проверок - удалять блок из старого места
- ❌ **ЗАПРЕЩЕНО** удалять блок до его вставки в новое место
- **ПРИЧИНА:** Если вставка не удастся, блок останется на старом месте (безопасность)
- **ПОРЯДОК:** ВСТАВКА → проверки → УДАЛЕНИЕ → проверки

**ОБМЕН МЕСТАМИ ДВУХ БЛОКОВ:**
Если нужно обменять местами два блока (например, два класса), выполнять операции последовательно:
1. **ВСТАВИТЬ** первый блок в новое место → проверить
2. **УДАЛИТЬ** первый блок из старого места → проверить
3. **ВСТАВИТЬ** второй блок в новое место → проверить
4. **УДАЛИТЬ** второй блок из старого места → проверить

**КРИТИЧНО:** Всегда сначала ВСТАВЛЯТЬ, потом УДАЛЯТЬ. Никогда не удалять блок до его вставки в новое место!

**ШАГИ:**

1. **ШАГ 1: ЧТЕНИЕ ИСХОДНОГО БЛОКА**
   - ✅ Прочитать исходный блок через `read_file` с достаточным контекстом
   - ✅ Определить начало и конец блока (класса, функции, блока кода)
   - ✅ Скопировать блок ТОЧНО, включая все пробелы и отступы

2. **ШАГ 2: ОПРЕДЕЛЕНИЕ МЕСТ ВСТАВКИ И УДАЛЕНИЯ**
   - ✅ Прочитать место вставки через `read_file` (минимум 50 строк)
   - ✅ Найти точное место, куда нужно вставить блок
   - ✅ Определить правильные отступы для нового блока
   - ✅ Прочитать старое место через `read_file` с достаточным контекстом
   - ✅ Определить контекст ДО и ПОСЛЕ блока в старом месте

3. **ШАГ 3: ПРОВЕРКА УНИКАЛЬНОСТИ КОНТЕКСТОВ**
   - ✅ **ОБЯЗАТЕЛЬНО** использовать `grep` для проверки уникальности контекста вставки
   - ✅ Команда: `grep -n "строка_перед_вставкой" файл.py` должна вернуть ТОЛЬКО одну строку
   - ✅ **ОБЯЗАТЕЛЬНО** использовать `grep` для проверки уникальности контекста удаления
   - ✅ Проверить уникальность строки ДО блока: `grep -n "строка_перед_блоком" файл.py`
   - ✅ Проверить уникальность начала блока: `grep -n "class BlockName" файл.py` должна вернуть ТОЛЬКО одну строку (старое место)
   - ✅ Если строка ДО блока не уникальна (встречается несколько раз), ОБЯЗАТЕЛЬНО использовать начало блока (`class ...` или `def ...`) в контексте для уникальности
   - ❌ **ЗАПРЕЩЕНО** применять `search_replace` без предварительной проверки уникальности

4. **ШАГ 4: ВСТАВКА БЛОКА В НОВОЕ МЕСТО**
   - ✅ Использовать `search_replace` с МИНИМАЛЬНЫМ контекстом для уникальной идентификации места вставки
   - ✅ **КРИТИЧНО:** Контекст используется ТОЛЬКО для поиска места, заменяется ТОЛЬКО сам блок целиком
   - ✅ Формат замены: `"строка_перед_вставкой\n"` → `"строка_перед_вставкой\n\nБЛОК_ЦЕЛИКОМ\n"`
   - ✅ Вставить блок целиком с правильными отступами (НЕ включать контекст в замену, только сам блок)
   - ✅ **ОБЯЗАТЕЛЬНО** проверить синтаксис: `python3 -m py_compile <файл>`
   - ✅ **ОБЯЗАТЕЛЬНО** проверить линтер: `read_lints`
   - ✅ **ОБЯЗАТЕЛЬНО** проверить фактическое расположение: `grep -n "class ИмяКласса" файл.py` должно показать оба места (старое и новое)
   - ✅ Только после успешных проверок - переход к следующему шагу (удаление)

5. **ШАГ 5: УДАЛЕНИЕ БЛОКА ИЗ СТАРОГО МЕСТА**
   - ✅ Использовать `search_replace` с МИНИМАЛЬНЫМ контекстом для уникальной идентификации места удаления
   - ✅ **КРИТИЧНО:** Контекст используется ТОЛЬКО для поиска места, удаляется ТОЛЬКО сам блок целиком
   - ✅ **ОБЯЗАТЕЛЬНО:** Включать начало блока (`class ...` или `def ...`) в контекст для уникальности, так как начало блока уникально и обеспечит точное нахождение места удаления
   - ✅ Формат замены: `"строка_перед_блоком\n\nclass BlockName:\n...БЛОК_ЦЕЛИКОМ...\n\nстрока_после_блока"` → `"строка_перед_блоком\n\nстрока_после_блока"`
   - ✅ Удалить блок целиком (НЕ включать контекст в замену, только удалить сам блок)
   - ✅ **ВАЖНО:** Если контекст ДО блока не уникален (встречается несколько раз), ОБЯЗАТЕЛЬНО использовать начало блока (`class ...` или `def ...`) для уникальности
   - ✅ **ОБЯЗАТЕЛЬНО** проверить синтаксис: `python3 -m py_compile <файл>`
   - ✅ **ОБЯЗАТЕЛЬНО** проверить линтер: `read_lints`
   - ✅ **ОБЯЗАТЕЛЬНО** проверить, что блок удален: `grep -n "class ИмяКласса" файл.py` должно вернуть ТОЛЬКО новое расположение
   - ✅ Только после успешных проверок - переход к следующей операции (если есть)

**ВАЖНО:** 
- `search_replace` работает с блоками ЛЮБОГО размера (включая 400+ строк). Нет необходимости разбивать большие блоки - можно перемещать целиком с полными проверками после операции.
- **КРИТИЧНО:** После КАЖДОГО `search_replace` выполнять ВСЕ три проверки: синтаксис + линтер + фактическое расположение.
- **КРИТИЧНО:** Перед КАЖДЫМ `search_replace` проверять уникальность контекста через `grep`.
- **КРИТИЧНО:** При вставке/удалении блока использовать МИНИМАЛЬНЫЙ контекст (1-2 строки) ТОЛЬКО для поиска места. В `search_replace` заменять/удалять ТОЛЬКО сам блок целиком, без изменения контекста. Это обеспечит, что редактор покажет один большой блок изменений вместо множества мелких.

**ПРИМЕРЫ ПРАВИЛЬНОГО ИСПОЛЬЗОВАНИЯ:**

**ПРИМЕР 1: Правильный перенос блока (полный процесс)**
```python
# ШАГ 4: ВСТАВКА БЛОКА В НОВОЕ МЕСТО
# Контекст ДО уникален (встречается 1 раз):
old_string = "print(\"WARNING\")\n\nclass NewClass(object):"
new_string = "print(\"WARNING\")\n\nclass BlockToMove(object):\n...весь блок...\n\nclass NewClass(object):"
# Проверки: синтаксис + линтер + расположение (должно быть 2 места)

# ШАГ 5: УДАЛЕНИЕ БЛОКА ИЗ СТАРОГО МЕСТА
# Контекст ДО не уникален (встречается 2 раза), но начало блока уникально:
old_string = "return filtered\n\nclass BlockToMove(object):\n...весь блок...\n\n# Версия приложения"
new_string = "return filtered\n\n# Версия приложения"
# Проверки: синтаксис + линтер + расположение (должно быть только новое место)
```

**ПРИМЕР 2: НЕПРАВИЛЬНОЕ удаление (без начала блока, контекст не уникален)**
```python
# ❌ НЕПРАВИЛЬНО: "return filtered" встречается 2 раза, может найти неправильное место!
old_string = "return filtered\n\n...весь блок...\n\n# Версия приложения"
new_string = "return filtered\n\n# Версия приложения"
# Правильно: включить начало блока для уникальности
old_string = "return filtered\n\nclass BlockToMove(object):\n...весь блок...\n\n# Версия приложения"
new_string = "return filtered\n\n# Версия приложения"
```

### 🔧 АЛГОРИТМ 7: ДОБАВЛЕНИЕ МЕТОДА В СЕРЕДИНУ КЛАССА

**ЦЕЛЬ:** Добавить новый метод в определенное место внутри класса (не в конец).

**ШАГИ:**

1. **ШАГ 1: ОПРЕДЕЛЕНИЕ МЕСТА ВСТАВКИ**
   - ✅ Прочитать область вставки через `read_file` (минимум 50 строк)
   - ✅ Найти метод, после которого нужно вставить новый метод
   - ✅ Определить правильные отступы для нового метода

2. **ШАГ 2: ПРОВЕРКА УНИКАЛЬНОСТИ КОНТЕКСТА**
   - ✅ Использовать `grep` для проверки, что контекст вставки уникален
   - ✅ Команда: `grep -n "def метод_перед_вставкой" файл.py` должна вернуть ТОЛЬКО одну строку
   - ✅ Если метод встречается несколько раз - добавить больше контекста (имя класса, соседние методы)

3. **ШАГ 3: ФОРМИРОВАНИЕ КОНТЕКСТА ДЛЯ ВСТАВКИ**
   - ✅ Включить минимум 50 строк уникального кода до и после места вставки
   - ✅ Включить конец метода перед вставкой и начало метода после вставки
   - ✅ Включить уникальные строки: имена методов, комментарии, специфичные значения

4. **ШАГ 4: ПРИМЕНЕНИЕ ВСТАВКИ**
   - ✅ Использовать `search_replace` с точным контекстом
   - ✅ Вставить новый метод между существующими методами
   - ✅ Сохранить все отступы в незатронутых частях кода

5. **ШАГ 5: ПРОВЕРКА РЕЗУЛЬТАТА**
   - ✅ Проверить синтаксис: `python3 -m py_compile <файл>`
   - ✅ Проверить линтер: `read_lints`
   - ✅ Проверить фактическое расположение: `grep -n "def новый_метод" файл.py`
   - ✅ Проверить порядок методов: `read_file` для области вставки
   - ✅ Убедиться, что новый метод находится между правильными методами

### 🎯 ПРИНЦИПЫ ДЛЯ ВСЕХ АЛГОРИТМОВ:

1. **ОДНО ИЗМЕНЕНИЕ ЗА РАЗ:** Никогда не делать несколько операций одновременно
2. **ПОЛНАЯ ПРОВЕРКА ПОСЛЕ КАЖДОГО ШАГА:** Синтаксис + линтер + фактическое расположение
3. **УНИКАЛЬНОСТЬ КОНТЕКСТА:** Всегда проверять через `grep` перед применением
4. **СОХРАНЕНИЕ ОТСТУПОВ:** Никогда не менять отступы в незатронутых частях кода
5. **ТОЧНОЕ СОВПАДЕНИЕ:** Копировать строки ТОЧНО из файла, не добавляя лишних пробелов

### ❌ ЗАПРЕЩЕНО:

- ❌ **КРИТИЧЕСКИ ЗАПРЕЩЕНО:** Использовать Python-скрипты (`fix_*.py`) для редактирования кода - изменения не видны в IDE и нельзя отменить!
- ❌ **КРИТИЧЕСКИ ЗАПРЕЩЕНО:** Использовать любые скрипты для редактирования кода - только `search_replace`!
- ❌ Пропускать проверки после операции
- ❌ Делать несколько операций одновременно
- ❌ Использовать контекст, который встречается более одного раза
- ❌ Менять отступы в незатронутых частях кода
- ❌ Продолжать после ошибок синтаксиса или линтера без исправления

## 🚨 КРИТИЧЕСКОЕ ПРАВИЛО: БЕЗОПАСНОЕ ИСПОЛЬЗОВАНИЕ search_replace (ПРОДОЛЖЕНИЕ)

### 🎯 ПРИНЦИП:
**"ПРОВЕРКА УНИКАЛЬНОСТИ + ТОЧНЫЙ КОНТЕКСТ + ПРОВЕРКА ПОСЛЕ = БЕЗОПАСНОЕ РЕДАКТИРОВАНИЕ"**
Ассистент использует `search_replace` только после проверки уникальности контекста и обязательной проверки результата!

## 🚨 КРИТИЧЕСКОЕ ПРАВИЛО: МЕТОД РЕДАКТИРОВАНИЯ КОДА

### 1. ЧТЕНИЕ И АНАЛИЗ КОДА:

- ✅ **Перед использованием search_replace ОБЯЗАТЕЛЬНО читать точные строки через read_file**
- ✅ **Использовать ТОЧНОЕ совпадение строк из файла в search_replace**
- ✅ **Копировать строки ТОЧНО из файла, не добавляя лишних пробелов**
- ✅ **Включать достаточный контекст для уникальной идентификации места замены**

### 2. ПОШАГОВЫЙ ПОДХОД:

- ✅ **Делать ОДНО изменение за раз**
- ✅ **После КАЖДОГО изменения проверять синтаксис: `python3 -m py_compile <файл>`**
- ✅ **После КАЖДОГО изменения проверять линтер: `read_lints`**
- ✅ **Только после успешной проверки переходить к следующему шагу**

### 3. СОХРАНЕНИЕ ОТСТУПОВ:

- ❌ **НИКОГДА не менять отступы в незатронутых частях кода**
- ✅ **Сохранять существующую структуру отступов**
- ✅ **Использовать ТОЧНОЕ совпадение строк из файла**

### 4. ЗАПРЕЩЕНО:

- ❌ **Использовать `search_replace` без соблюдения правил безопасности** (см. раздел "🚨 БЕЗОПАСНОЕ ИСПОЛЬЗОВАНИЕ search_replace")
- ❌ **Использовать `MultiEdit`** (слишком рискованно для больших файлов)
- ❌ **Использовать команды терминала (sed, awk, perl) для редактирования кода** (ИСКЛЮЧЕНИЕ: `sed` разрешён ТОЛЬКО при создании снимков (коммитов) по правилам из раздела "📁 ПРАВИЛО ДЛЯ ИСТОРИИ КОММИТОВ" для массового обновления версий/дат)
- ❌ **Делать массовые автоматические исправления отступов**
- ❌ **Пропускать проверки синтаксиса и линтера**

### 5. ПРИ ОШИБКАХ:

- ✅ **Если появились ошибки синтаксиса - исправлять их точечно, по одной**
- ✅ **Если появились ошибки отступов - перечитать файл и исправить ТОЛЬКО проблемное место**
- ❌ **НИКОГДА не применять массовые исправления**

### 🎯 ПРИНЦИП:
**"search_replace С ПРОВЕРКОЙ УНИКАЛЬНОСТИ + ТОЧНОЕ СОВПАДЕНИЕ + ПРОВЕРКА СИНТАКСИСА И ЛИНТЕРА = БЕЗОПАСНОЕ РЕДАКТИРОВАНИЕ"**
Ассистент использует `search_replace` с обязательной проверкой уникальности контекста и проверкой результата после каждого изменения!

## 🚨 КРИТИЧЕСКОЕ ПРАВИЛО: ВЫБОР МЕТОДА РЕДАКТИРОВАНИЯ КОДА

### ✅ ЕДИНСТВЕННЫЙ РАЗРЕШЕННЫЙ МЕТОД: `search_replace`

**ОСНОВНОЙ И ЕДИНСТВЕННЫЙ МЕТОД: `search_replace`**
- ✅ **ОБЯЗАТЕЛЬНО** использовать `search_replace` для ВСЕХ операций с кодом
- ✅ Интегрируется с IDE - изменения видны в Cursor, можно отменять (Ctrl+Z)
- ✅ Работает с любым размером файла (включая большие блоки 400+ строк)
- ✅ Требует проверки уникальности контекста через `grep` перед применением
- ✅ См. раздел "🚨 БЕЗОПАСНОЕ ИСПОЛЬЗОВАНИЕ search_replace" для детальных правил
- ✅ См. раздел "🚨 КРИТИЧЕСКОЕ ПРАВИЛО: АЛГОРИТМЫ ОПЕРАЦИЙ С КОДОМ" для алгоритмов операций

**КРИТИЧЕСКИ ЗАПРЕЩЕНО:**
- ❌ **КРИТИЧЕСКИ ЗАПРЕЩЕНО** использовать Python-скрипты (`fix_*.py`) для редактирования кода - изменения не видны в IDE и нельзя отменить!
- ❌ **КРИТИЧЕСКИ ЗАПРЕЩЕНО** использовать любые скрипты для редактирования кода - только `search_replace`!
- ❌ **ЗАПРЕЩЕНО** использовать `MultiEdit` (слишком рискованно)
- ❌ **ЗАПРЕЩЕНО** использовать `sed`, `awk`, `perl` для редактирования кода (ИСКЛЮЧЕНИЕ: только для создания коммитов - обновление версий/дат)

**ИСКЛЮЧЕНИЯ:**
- ✅ `sed` разрешён ТОЛЬКО при создании снимков (коммитов) по правилам из раздела "📁 ПРАВИЛО ДЛЯ ИСТОРИИ КОММИТОВ" для массового обновления версий/дат во всех файлах проекта

### 🎯 ПРИНЦИП:
**"ВСЕ ОПЕРАЦИИ С КОДОМ - ТОЛЬКО search_replace + ПРОВЕРКА УНИКАЛЬНОСТИ + ПРОВЕРКА СИНТАКСИСА + ПРОВЕРКА ЛИНТЕРА + ПРОВЕРКА ФАКТИЧЕСКОГО РЕЗУЛЬТАТА"**
Ассистент использует ТОЛЬКО `search_replace` для ВСЕХ операций с кодом (добавление, удаление, изменение, перемещение) и проверяет результат после каждого изменения!

## 🎯 ПРАВИЛО ДЛЯ УКАЗАНИЯ МЕСТ В КОДЕ:

### ✅ ИСПОЛЬЗОВАТЬ КОМБИНИРОВАННЫЙ ПОДХОД:
- **Номера строк:** Указывать для ориентировочного позиционирования (формат: ~номер)
- **Названия методов:** Дополнять для контекста
- **Ключевые слова:** Использовать для поиска
- **Контекстные фразы:** Применять для понимания

### ⚠️ ВАЖНО О НОМЕРАХ СТРОК:
- **ОРИЕНТИРОВОЧНЫЕ:** Номера строк могут сдвигаться при изменениях в коде
- **ФОРМАТ:** Использовать символ ~ перед номером (например: ~8223)
- **ДЛЯ ПОИСКА:** Использовать в сочетании с контекстными ориентирами
- **ПРОВЕРЯТЬ:** Всегда проверять актуальность номеров строк

### 🔍 ПРИМЕРЫ ПРАВИЛЬНОГО УКАЗАНИЯ:
- ✅ **ПРАВИЛЬНО:** "строка ~8223 в методе start_automation"
- ✅ **ПРАВИЛЬНО:** "строка ~10420 с вызовом apt-get update"
- ✅ **ПРАВИЛЬНО:** "функция def run_automation (строка ~8287)"
- ✅ **ПРАВИЛЬНО:** "команда apt-get dist-upgrade (строка ~10435)"

### 🎯 ПРИНЦИП:
**"НОМЕРА СТРОК + КОНТЕКСТНЫЕ ОРИЕНТИРЫ = МАКСИМАЛЬНАЯ ТОЧНОСТЬ"**
Ассистент указывает места в коде через номера строк И контекстные ориентиры для лучшего понимания!

## 📝 ПРАВИЛА КОММИТОВ:
- Все коммиты на русском языке с кириллицей
- **ОБЯЗАТЕЛЬНО СПРАШИВАТЬ РАЗРЕШЕНИЕ НА КОММИТ!** Никогда не создавать коммиты без явного разрешения пользователя
- Сначала предложить создать коммит, объяснить что будет зафиксировано, и только после получения разрешения выполнять `git commit`
- Использовать файл сообщения: `git commit -F filename`

## 🏷️ ПРАВИЛА ВЕРСИОНИРОВАНИЯ:

### Схема версий:
- **Версия проекта**: `VMAJOR.MINOR.PATCH` (MAJOR/MINOR из `version.txt`, PATCH = номер коммита) - общая для всех файлов проекта
- **Дата релиза файла**: `YYYY.MM.DD` - индивидуальная для каждого файла

### Формат в файлах:
```bash
# Версия проекта: VMAJOR.MINOR.PATCH
# Дата релиза: YYYY.MM.DD
```

### Правила обновления:
1. **При изменении функциональности проекта** → обновляем версию проекта во ВСЕХ файлах
2. **При изменении конкретного файла** → обновляем дату релиза ТОЛЬКО в этом файле
3. **При изменении правил ASSISTANT_RULES.md** → обновляем версию проекта во ВСЕХ файлах
4. **MAJOR версия** (VVMAJOR.X.X) - кардинальные изменения архитектуры
5. **MINOR версия** (X.MINOR.X) - новые функции, улучшения
6. **PATCH версия** (X.X.PATCH) - номер коммита (сохраненных изменений)

### Примеры:
```
V2.3.76 (2025.11.20) - текущая версия проекта (76 коммитов)
├── FSA-AstraInstall.py: V2.3.76 (2025.11.20) ← изменен сегодня
├── FSA-AstraInstall: V2.3.76 (2025.11.20) ← изменен вчера
├── ASSISTANT_RULES.md: V2.3.76 (2025.11.20) ← изменен сегодня
└── README.md: V2.3.76 (2025.11.20) ← обновлен сегодня
```

## 📁 ПРАВИЛО ДЛЯ ИСТОРИИ КОММИТОВ:
**ОБЯЗАТЕЛЬНО ПОСЛЕ КАЖДОГО КОММИТА:** Создавать локальную историю с двумя скриптами проекта:

### 🔄 АЛГОРИТМ СОЗДАНИЯ СНИМКА:

**⚠️ ВАЖНО: ТОЧКИ БЕЗОПАСНОГО ПРЕРЫВАНИЯ:**
- **БЕЗОПАСНО прервать БЕЗ изменения файлов проекта:** До шага 9 (включительно шаг 8.5)
  - Шаги 0-8.5: только чтение файлов и создание временных файлов (`commit_message.txt`, `.commit_vars.sh`)
  - Переменные сохраняются в `.commit_vars.sh` сразу после определения в шагах 1, 3, 4 (функция `save_vars_to_file()`)
  - Файлы проекта НЕ изменяются
  - Можно безопасно прервать в любой момент
- **ПРЕРЫВАНИЕ С ИЗМЕНЕНИЕМ ФАЙЛОВ (требует отката):** После шага 9 или 11
  - Шаг 9: изменяет даты релиза в файлах проекта (через `sed -i`)
  - Шаг 11: изменяет версии во всех файлах проекта (через `sed -i`)
  - Если прервать после этих шагов, нужно откатить изменения: `git checkout -- *.py *.sh *.md`
- **ПОСЛЕ КОММИТА:** Шаги 13-20 не изменяют файлы проекта (только git операции и копирование)

0. **Проверка директории:** 
   - Выполнить `pwd` для проверки текущей директории
   - Если `basename $(pwd)` != "FSA-AstraInstall", выполнить `cd /Volumes/FSA-PRJ/Project/FSA-AstraInstall`
   - При ошибке перехода - остановиться с ошибкой

1. **Определение измененных файлов:** 
   - Выполнить `git diff --name-only HEAD` → сохранить в `CHANGED_FILES`
   - Выполнить `git status --short | grep "^??"` → извлечь новые файлы и директории
   - Разделить на `NEW_FILES` (файлы) и `NEW_DIRS` (директории) - проверкой `[ -f "$path" ]` и `[ -d "$path" ]`
   - Выполнить `git diff --name-only --diff-filter=D HEAD` → сохранить в `DELETED_FILES`
   - Если все переменные пусты - остановиться с ошибкой "Нет изменений для коммита"
   - **КРИТИЧНО:** Сохранить переменные в `.commit_vars.sh` через функцию `save_vars_to_file()` сразу после определения

2. **Проверка реальных изменений (внутренняя):** 
   - Для каждого файла из `CHANGED_FILES` (использовать `echo "$CHANGED_FILES" | tr ' ' '\n' | while read file` для правильного разделения строки):
     - Выполнить `git diff HEAD "$file"` → отфильтровать строки с "Версия|Version|дата|Date" → сохранить в временный файл (НЕ выводить в stdout)
   - Проверить, что есть реальные изменения кода (не только версии/даты)
   - Если нет реальных изменений - остановиться с ошибкой "Нет реальных изменений кода, только версии/даты"
   - Пользователь НЕ видит этот вывод

3. **Определение текущей версии:** 
   - **КРИТИЧНО: Использовать `git ls-files` вместо `find`** - это автоматически исключает файлы из `.gitignore` и гарантирует работу только в текущем проекте
   - Для каждого файла `*.py`, `*.sh`, `*.md` из отслеживаемых git (через `git ls-files | grep -E '\.(py|sh|md)$'`):
     - Выполнить `head -20 "$f" | grep "V[0-9]\+\.[0-9]\+\.[0-9]\+"` → сохранить в временный файл
   - Из временного файла извлечь все уникальные версии → сохранить в `ALL_VERSIONS` (строка через пробел)
   - Взять первую версию из `ALL_VERSIONS` → сохранить в `CURRENT_VERSION`
   - Если `CURRENT_VERSION` пуста - остановиться с ошибкой "Не удалось найти версию в файлах проекта"
   - Вывести: "Найдены версии в файлах: $ALL_VERSIONS" и "Основная версия для замены: $CURRENT_VERSION"
   - **КРИТИЧНО:** Сохранить переменные в `.commit_vars.sh` через функцию `save_vars_to_file()` сразу после определения

4. **Определение новой версии:** 
   - Прочитать `MAJOR` из `version.txt`: `grep "^MAJOR=" version.txt | cut -d= -f2`
   - Прочитать `MINOR` из `version.txt`: `grep "^MINOR=" version.txt | cut -d= -f2`
   - Если `MAJOR` или `MINOR` пусты - остановиться с ошибкой
   - **КРИТИЧНО:** Сохранить переменную `NEW_VERSION` в `.commit_vars.sh` через функцию `save_vars_to_file()` сразу после определения
   - Выполнить `git rev-list --count HEAD` → сохранить в `CURRENT_COMMIT`
   - Вычислить `NEW_PATCH = CURRENT_COMMIT + 1`
   - Сформировать `NEW_VERSION = "V${MAJOR}.${MINOR}.${NEW_PATCH}"`
   - Если `NEW_VERSION` пуста или равна "V.." - остановиться с ошибкой
   - Вывести: "Новая версия: $NEW_VERSION (текущая: $CURRENT_VERSION)"

5. **Сбор подробной информации об изменениях:** 
   - Сохранить данные во временный файл `.commit_analysis_data.txt` (НЕ выводить в stdout)
   - Для каждого файла из `CHANGED_FILES` (использовать `echo "$CHANGED_FILES" | tr ' ' '\n' | while read file` для правильного разделения строки):
     - Выполнить `git diff --stat HEAD "$file"` → сохранить статистику в файл
     - Выполнить `git diff HEAD "$file"` → отфильтровать строки с "Версия|Version|дата|Date"` → сохранить первые 100 строк в файл
   - Вывести ТОЛЬКО: "=== Сбор данных для анализа... ==="
   - Вывести: "=== Ассистент анализирует изменения и формирует описание коммита... ==="
   - Вывести маркер "⏸ ПАУЗА: Ассистент должен остановиться здесь"

6. **Анализ изменений ассистентом:** 
   - Ассистент ОБЯЗАН остановиться после шага 5
   - Прочитать данные из файла `.commit_analysis_data.txt`
   - Проанализировать все изменения в файлах
   - Сформировать краткое понятное описание на русском языке в строго определенном формате (см. шаг 7)
   - Удалить временный файл `.commit_analysis_data.txt` после чтения
   - Пользователь НЕ видит процесс анализа

7. **Сохранение описания:** 
   - Определить текущую дату: `TODAY=$(date +%Y.%m.%d)`
   - Формат описания СТРОГО определен:
     ```
     [Заголовок коммита - опишите кратко основные изменения]
     
     Кратко, что изменили (списком):
     
     Имя_файла1:
     - Краткое описание изменения 1
     - Краткое описание изменения 2
     
     Имя_файла2:
     - Краткое описание изменения 1
     
     Описание удаленных классов/модулей:
     - Нет удаленных компонентов (или описание если есть)
     
     Проект: FSA-AstraInstall
     Дата: $TODAY
     ```
   - **КРИТИЧНО: НЕ комментировать изменения дат и версий в файлах проекта** - эти изменения делаются автоматически алгоритмом (шаги 9 и 11) и не должны упоминаться в описании коммита
   - В описании указывать ТОЛЬКО реальные изменения кода, логики, правил, структуры и т.д.
   - Сохранить описание в файл `commit_message.txt` ТОЛЬКО через `echo`/`sed` (ЗАПРЕЩЕНО использовать `write` tool)
   - В строке "Дата:" использовать переменную `$TODAY`, а НЕ статическую дату
   - Пример команды: `echo "[Заголовок]" > commit_message.txt && echo "" >> commit_message.txt && ... && echo "Дата: $TODAY" >> commit_message.txt`
   - ТОЛЬКО после сохранения файла продолжать выполнение шага 8

8. **Проверка файла сообщения:** 
   - Проверить: `[ -f "commit_message.txt" ] && [ -s "commit_message.txt" ]`
   - Если файл не существует или пуст - остановиться с ошибкой "Файл commit_message.txt не найден или пуст"

8.5. **ОБЯЗАТЕЛЬНАЯ ПАУЗА:** 
   - Вывести: "=== Описание коммита готово ==="
   - Вывести содержимое файла `commit_message.txt` через `cat commit_message.txt`
   - **КРИТИЧНО:** Переменные уже сохранены в файл `.commit_vars.sh` в шагах 1, 3, 4 (функция `save_vars_to_file()`)
   - Проверить наличие файла `.commit_vars.sh` и обновить его с актуальными значениями (вызов `save_vars_to_file()`)
   - Вывести маркер "🚨 КРИТИЧНО: ОБЯЗАТЕЛЬНАЯ ПАУЗА ДЛЯ АССИСТЕНТА 🚨"
   - Ассистент ОБЯЗАН остановиться и НЕ продолжать автоматически
   - Ассистент ДОЛЖЕН спросить пользователя через чат: "Файл commit_message.txt готов. Нужно что-то изменить?"
   - **МЕХАНИЗМ РАЗРЕШЕНИЯ:** Пользователь отвечает в чате одним из способов:
     - "продолжай" или "продолжи" - разрешает продолжение
     - "+" - разрешает продолжение
     - "да" или "ок" - разрешает продолжение
     - "измени" или указание на конкретные изменения - ассистент редактирует файл и снова спрашивает
     - Любой другой ответ - ассистент интерпретирует как разрешение или уточняет намерение
   - ТОЛЬКО после получения ответа пользователя в чате ассистент продолжает выполнение шага 9
   - **⚠️ ПОСЛЕДНЯЯ ТОЧКА БЕЗОПАСНОГО ПРЕРЫВАНИЯ БЕЗ ИЗМЕНЕНИЯ ФАЙЛОВ ПРОЕКТА**

9. **Обновление дат релиза:** 
   - Загрузить переменные из `.commit_vars.sh`: `source .commit_vars.sh`
   - Проверить: `[ -f "commit_message.txt" ]` - если нет, остановиться с ошибкой
   - Определить `TODAY = $(date +%Y.%m.%d)`
   - Для каждого файла из `CHANGED_FILES` (использовать `echo "$CHANGED_FILES" | tr ' ' '\n' | while read file` для правильного разделения строки), если файл имеет расширение `.py`, `.sh` или `.md`:
     - Выполнить `sed -i '' "1,20s/([0-9]\{4\}\.[0-9]\{2\}\.[0-9]\{2\})/($TODAY)/g" "$file"` (ограничение первыми 20 строками)
     - Также обновить даты в кавычках: `sed -i '' "1,20s/\"V[0-9]\+\.[0-9]\+\.[0-9]\+ ([0-9]\{4\}\.[0-9]\{2\}\.[0-9]\{2\})\"/\"V[0-9]\+\.[0-9]\+\.[0-9]\+ ($TODAY)\"/g" "$file"` (ограничение первыми 20 строками)
   - **⚠️ ИЗМЕНЯЕТ ФАЙЛЫ ПРОЕКТА**

10. **Проверка обновления дат:** 
   - Для каждого файла из `CHANGED_FILES` (использовать `echo "$CHANGED_FILES" | tr ' ' '\n' | while read file` для правильного разделения строки), если файл имеет расширение `.py`, `.sh` или `.md`:
     - Выполнить `head -20 "$file" | grep -qF "($TODAY)"` (проверка только первых 20 строк)
     - Если не найдено - зафиксировать ошибку
   - Если есть ошибки - остановиться с ошибкой "Дата релиза не была обновлена в N файл(ах)"
   - Если все даты обновлены - вывести "✓ Все даты релиза успешно обновлены"

11. **Обновление версий проекта:** 
   - Загрузить переменные из `.commit_vars.sh`: `source .commit_vars.sh` (КРИТИЧНО: без переменных продолжать нельзя)
   - В файле `.commit_vars.sh` должны быть сохранены: `CHANGED_FILES`, `NEW_FILES`, `NEW_DIRS`, `DELETED_FILES`, `CURRENT_VERSION`, `NEW_VERSION`, `ALL_VERSIONS`
   - Проверить `NEW_VERSION` - если пуста, остановиться с ошибкой
   - Загрузить `MAJOR` и `MINOR` из `version.txt`
   - **КРИТИЧНО: Использовать `git ls-files` вместо `find`** - это автоматически исключает файлы из `.gitignore` и гарантирует работу только в текущем проекте
   - Для каждого файла `*.py`, `*.sh`, `*.md` из отслеживаемых git (через `git ls-files | grep -E '\.(py|sh|md)$'`):
     - **КРИТИЧНО:** Проверить, что `NEW_VERSION` не пуста перед выполнением замены (если пуста, пропустить файл с ошибкой)
     - Заменить **ЛЮБУЮ** версию формата `V[0-9]+.[0-9]+.[0-9]+` на `NEW_VERSION` в первых 20 строках: `sed -i '' "1,20s/V[0-9][0-9]*\\.[0-9][0-9]*\\.[0-9][0-9]*/${NEW_VERSION}/g" "$file"` (с подавлением ошибок `2>/dev/null || true`)
     - **КРИТИЧНО:** В macOS sed не поддерживает `\+`, используем `[0-9][0-9]*` для одного или более цифр
     - **КРИТИЧНО:** Использовать паттерн напрямую в команде sed (не через переменную), чтобы избежать проблем с подстановкой переменных и экранированием
     - Выводить прогресс обработки: `[N/Total] Обработка: $file`
   - **⚠️ ИЗМЕНЯЕТ ФАЙЛЫ ПРОЕКТА**

12. **Проверка обновления версий:** 
   - **КРИТИЧНО: Использовать `git ls-files` вместо `find`** - это автоматически исключает файлы из `.gitignore` и гарантирует работу только в текущем проекте
   - Для каждого файла `*.py`, `*.sh`, `*.md` из отслеживаемых git (через `git ls-files | grep -E '\.(py|sh|md)$'`):
     - Выполнить `head -20 "$file" | grep -q "$NEW_VERSION"`
     - Если найдено - вывести "✓ Версия обновлена в $file"
     - Если не найдено:
       - Выполнить `head -20 "$file" | grep -q "V[0-9][0-9]*\\.[0-9][0-9]*\\.[0-9][0-9]*"` (проверка наличия любой версии)
       - Если найдена версия - зафиксировать ошибку с указанием найденной старой версии (извлечь через `grep -o "V[0-9][0-9]*\\.[0-9][0-9]*\\.[0-9][0-9]*"`)
       - Если не найдено - вывести "ℹ Файл $file не содержит версию в начале файла (пропускаем)"
   - Если есть ошибки - остановиться с ошибкой "Версия не обновлена в N файл(ах)"
   - Если все версии обновлены - продолжить

13. **Добавление файлов в индекс:** 
   - Для каждого файла из `CHANGED_FILES` (использовать `echo "$CHANGED_FILES" | tr ' ' '\n' | while read file`): выполнить `git add "$file"`
   - Для каждого файла из `NEW_FILES` (использовать `echo "$NEW_FILES" | tr ' ' '\n' | while read file`): выполнить `git add "$file"`
   - Для каждой директории из `NEW_DIRS` (использовать `echo "$NEW_DIRS" | tr ' ' '\n' | while read dir`): выполнить `git add "$dir"` (рекурсивно добавит все файлы)
   - Для каждого файла из `DELETED_FILES` (использовать `echo "$DELETED_FILES" | tr ' ' '\n' | while read file`): выполнить `git add "$file"`
   - **КРИТИЧНО: Использовать `git ls-files` вместо `find`** для поиска измененных файлов с версиями
   - Для каждого файла `*.py`, `*.sh`, `*.md` из отслеживаемых git (через `git ls-files | grep -E '\.(py|sh|md)$'`), если он был изменен версией/датой: выполнить `git add "$file"`
   - ЗАПРЕЩЕНО использовать `git add .`
   - Выполнить `git status --short` для проверки
   - Если нет файлов в индексе - остановиться с ошибкой

14. **Создание коммита:** 
   - Выполнить `git commit -F commit_message.txt`
   - Если коммит не создан (exit code != 0) - остановиться с ошибкой

15. **Верификация коммита:** 
   - Выполнить `git log -1 --stat` для проверки
   - Выполнить `git log -1 --format="%H"` для получения хеша коммита
   - Если коммит не найден - остановиться с ошибкой
   - Вывести информацию о коммите

16. **Очистка:** 
   - Удалить `commit_message.txt`: `rm -f commit_message.txt`
   - Удалить `.commit_vars.sh`: `rm -f .commit_vars.sh`
   - Удалить `.commit_analysis_data.txt`: `rm -f .commit_analysis_data.txt` (если остался)

17. **Проверка необходимости истории:** 
   - Выполнить `git log -1 --name-only --format=""` → сохранить список файлов из последнего коммита
   - Проверить: есть ли в списке файлы `FSA-AstraInstall.py`, `FSA-AstraInstall-1-7` или `FSA-AstraInstall-1-8`
   - Если есть хотя бы один - установить `NEED_HISTORY=yes`
   - Если нет ни одного - установить `NEED_HISTORY=no`
   - **КРИТИЧНО:** Использовать данные из git, а НЕ из переменных (файл `.commit_vars.sh` уже удален на шаге 16)

18. **Создание директории и копирование файлов (условно):** 
   - ТОЛЬКО если `NEED_HISTORY=yes`:
     - Определить `COMMIT_DATE = $(date +%Y_%m_%d_%H_%M)`
     - Создать директорию: `mkdir -p "History/$COMMIT_DATE"`
     - Скопировать файлы: `cp FSA-AstraInstall.py FSA-AstraInstall-1-7 FSA-AstraInstall-1-8 "History/$COMMIT_DATE/" 2>/dev/null || true`
   - Если `NEED_HISTORY=no` - пропустить этот шаг

19. **Валидация истории (условно):** 
   - ТОЛЬКО если `NEED_HISTORY=yes`:
     - Выполнить `ls -la "History/$COMMIT_DATE/"` для проверки
     - Если файлы не скопированы - остановиться с ошибкой
   - Если `NEED_HISTORY=no` - пропустить этот шаг

20. **Подтверждение завершения и отчёт:** 
   - Вывести: "=== Снимок (коммит) успешно создан! ==="
   - Вывести отчёт о проделанной работе:
     - Хеш коммита: `git log -1 --format="%H"`
     - Краткое сообщение коммита: `git log -1 --format="%s"`
     - Изменённые файлы: `git diff --name-only HEAD~1 HEAD`
     - Статистика изменений: `git diff --stat HEAD~1 HEAD`
     - Если создана история (`NEED_HISTORY=yes`) - указать путь к директории истории: `History/$COMMIT_DATE/`
   - Использовать ТОЛЬКО простой `echo`, без сложных конструкций

### 📋 КОМАНДЫ ДЛЯ ВЫПОЛНЕНИЯ:

**⚠️ ВАЖНО: Избегание зависаний команд:**
- Используйте простой `echo` вместо heredoc (`<<EOF`) для создания файлов
- Избегайте сложных regex в pipe (`|`) - используйте простые проверки
- Разбивайте сложные команды на несколько простых
- Используйте простые `if/elif` вместо сложных `grep -Eq` с regex в pipe

**🚨 КРИТИЧНО: ОБЪЕДИНЕНИЕ КОМАНД ТОЛЬКО ВНУТРИ ОДНОГО ШАГА:**
- **ВАЖНО:** Команды разных шагов НЕ объединяются - каждый шаг выполняется отдельно для возможности проверки результата
- **ОБЪЕДИНЯТЬ** можно только команды ВНУТРИ одного шага через `&&` или `;`
- **ПРИМЕРЫ объединения внутри шага:**
  - Несколько `echo` в одном шаге: `echo "Текст1" && echo "Текст2" && echo "Текст3"`
  - Создание файла и запись в одном шаге: `echo "Содержимое" > file.txt && echo "Дополнительно" >> file.txt`
- **ЗАПРЕЩЕНО** объединять команды разных шагов - между шагами должны быть четкие границы
- **ПРИОРИТЕТ:** Возможность проверить результат каждого шага перед переходом к следующему

**🚨 КРИТИЧНО: ЗАПРЕТ ДЛИННЫХ КОМАНД:**
- **ЗАПРЕЩЕНО** выполнять команды длиннее 200 символов в одной строке
- **ОБЯЗАТЕЛЬНО** разбивать длинные команды на несколько коротких команд
- Если команда содержит более 3-4 операторов `&&` или `||` - разбить на отдельные команды
- Если команда содержит сложные pipe-цепочки с более чем 2-3 командами - разбить на части через временные файлы
- **ПРИОРИТЕТ:** Надежность выполнения важнее скорости - лучше 5 коротких команд, чем 1 длинная, которая зависнет

**🚨 КРИТИЧНО: ОБЯЗАТЕЛЬНОЕ ПОВТОРЕНИЕ ШАГОВ ДО УСПЕШНОГО ЗАВЕРШЕНИЯ:**
- **КАЖДЫЙ ШАГ** алгоритма создания снимка ДОЛЖЕН быть выполнен успешно (exit code 0)
- **ЗАПРЕЩЕНО ПРОПУСКАТЬ ШАГИ** - все шаги с 0 по 20 должны быть выполнены строго по порядку
- **ОБЯЗАТЕЛЬНАЯ ПРОВЕРКА УСПЕШНОСТИ:** После каждого шага ДОЛЖНА быть явная проверка успешности выполнения:
  - Проверка exit code: `if [ $? -ne 0 ]; then stop_on_error "Ошибка в шаге X"; fi`
  - Проверка наличия ожидаемого результата (файл, переменная, вывод)
  - ТОЛЬКО после успешной проверки переход к следующему шагу
- **ЕСЛИ** команда была прервана (Command was canceled) - **ЛОГИКА ПОВТОРЕНИЯ:**
  1. **ПЕРВАЯ попытка прерывания:** Ассистент МОЖЕТ автоматически повторить команду с упрощением (разбить на части, убрать сложные pipe-цепочки)
  2. **ВТОРАЯ и последующие попытки прерывания:** Ассистент ДОЛЖЕН остановиться, сообщить пользователю и спросить, как действовать дальше
  3. **ПРИОРИТЕТ:** Если команда явно зависла из-за длины/сложности - автоматически упростить и повторить. Если прерывание повторяется - спросить пользователя
- **ЕСЛИ** команда завершилась с ошибкой (exit code != 0) - **ОБЯЗАТЕЛЬНО** проанализировать ошибку, исправить проблему и повторить
- **ЕСЛИ** команда зависла (не отвечает длительное время, все еще выполняется) - ассистент ДОЛЖЕН сообщить пользователю и спросить, прервать ли команду (нельзя автоматически повторить, пока команда не завершена)
- **ЗАПРЕЩЕНО** продолжать выполнение следующих шагов, если предыдущий шаг не завершился успешно
- **ЗАПРЕЩЕНО** пропускать шаги или переходить к следующему шагу после прерывания команды
- **ПРИ ПОВТОРЕНИИ:** Ассистент ДОЛЖЕН выполнить ту же команду (или её упрощенную версию), а НЕ переходить к следующему шагу
- **ПРОВЕРКА УСПЕХА:** После каждого шага проверять exit code и наличие ожидаемого вывода перед переходом к следующему шагу

**⚠️ КРИТИЧНО: ПРОВЕРКА ДИРЕКТОРИИ:**
- **ВСЕГДА** проверять текущую директорию перед выполнением команд: `pwd`
- **ВСЕГДА** использовать `cd /Volumes/FSA-PRJ/Project/FSA-AstraInstall &&` перед командами, которые работают с файлами
- **ВСЕГДА** проверять существование файлов перед операциями: `[ -f "$file" ] || continue`

**🚨 КРИТИЧНО: ОБЯЗАТЕЛЬНЫЕ ПАУЗЫ В СКРИПТАХ:**
- Если в выводе команды есть маркер **"🚨 КРИТИЧНО: ОБЯЗАТЕЛЬНАЯ ПАУЗА ДЛЯ АССИСТЕНТА 🚨"** или **"⏸ ПАУЗА: Ожидание разрешения пользователя"**
- **АССИСТЕНТ ОБЯЗАН ОСТАНОВИТЬСЯ** и НЕ продолжать выполнение следующих команд автоматически
- Ассистент должен показать пользователю содержимое вывода и спросить разрешения на продолжение
- **ТОЛЬКО** после получения явного разрешения пользователя (через чат) ассистент может продолжить выполнение
- **ЗАПРЕЩЕНО** выполнять следующие команды без разрешения пользователя
- **ВАЖНО:** Переменные сохраняются в файл `.commit_vars.sh` сразу после определения в шагах 1, 3, 4 (функция `save_vars_to_file()`). Перед продолжением после паузы ассистент ДОЛЖЕН загрузить их: `source .commit_vars.sh` или `. .commit_vars.sh`

```bash
# ШАГ 0: КРИТИЧНО: Проверка и переход в директорию проекта
echo "=== ШАГ 0: Проверка директории ==="
PROJECT_DIR="/Volumes/FSA-PRJ/Project/FSA-AstraInstall"
if [ "$(basename $(pwd))" != "FSA-AstraInstall" ]; then
    cd "$PROJECT_DIR" || {
        echo "❌ ОШИБКА: Не удалось перейти в директорию проекта"
        exit 1
    }
fi
# КРИТИЧНО: Дополнительная проверка через абсолютный путь
if [ "$(pwd)" != "$PROJECT_DIR" ]; then
    cd "$PROJECT_DIR" || {
        echo "❌ ОШИБКА: Не удалось перейти в директорию проекта"
        exit 1
    }
fi
echo "✓ Рабочая директория: $(pwd)"
if [ "$(basename $(pwd))" != "FSA-AstraInstall" ] || [ "$(pwd)" != "$PROJECT_DIR" ]; then
    echo "❌ ОШИБКА: Проверка директории не прошла"
    exit 1
fi
# КРИТИЧНО: Проверка успешности шага 0
if [ $? -ne 0 ]; then
    stop_on_error "ШАГ 0 не выполнен успешно"
fi

# КРИТИЧНО: Функция для остановки процесса при ошибках
# ВАЖНО: При автоматическом выполнении ассистентом функция просто завершает выполнение
# Ассистент увидит ошибку в выводе и сообщит пользователю через чат
stop_on_error() {
    local error_message="$1"
    echo ""
    echo "❌ ОШИБКА: $error_message"
    echo "⏸ Процесс остановлен. Ассистент сообщит пользователю об ошибке."
    echo "Пользователь должен исправить ошибку и попросить ассистента продолжить."
        exit 1
}

# КРИТИЧНО: Функция для сохранения переменных в файл
# Сохраняет переменные сразу после их определения, чтобы они были доступны в разных сессиях shell
# Перезаписывает файл полностью с актуальными значениями всех переменных
save_vars_to_file() {
    local VARS_FILE=".commit_vars.sh"
    # Перезаписываем файл с актуальными значениями всех переменных
    echo "# Переменные для продолжения создания коммита" > "$VARS_FILE"
    echo "# Этот файл будет удален после завершения процесса" >> "$VARS_FILE"
    [ ! -z "$CHANGED_FILES" ] && echo 'CHANGED_FILES="'$CHANGED_FILES'"' >> "$VARS_FILE" || echo 'CHANGED_FILES=""' >> "$VARS_FILE"
    [ ! -z "$NEW_FILES" ] && echo 'NEW_FILES="'$NEW_FILES'"' >> "$VARS_FILE" || echo 'NEW_FILES=""' >> "$VARS_FILE"
    [ ! -z "$NEW_DIRS" ] && echo 'NEW_DIRS="'$NEW_DIRS'"' >> "$VARS_FILE" || echo 'NEW_DIRS=""' >> "$VARS_FILE"
    [ ! -z "$DELETED_FILES" ] && echo 'DELETED_FILES="'$DELETED_FILES'"' >> "$VARS_FILE" || echo 'DELETED_FILES=""' >> "$VARS_FILE"
    [ ! -z "$CURRENT_VERSION" ] && echo 'CURRENT_VERSION="'$CURRENT_VERSION'"' >> "$VARS_FILE" || echo 'CURRENT_VERSION=""' >> "$VARS_FILE"
    [ ! -z "$NEW_VERSION" ] && echo 'NEW_VERSION="'$NEW_VERSION'"' >> "$VARS_FILE" || echo 'NEW_VERSION=""' >> "$VARS_FILE"
    [ ! -z "$ALL_VERSIONS" ] && echo 'ALL_VERSIONS="'$ALL_VERSIONS'"' >> "$VARS_FILE" || echo 'ALL_VERSIONS=""' >> "$VARS_FILE"
}

# ШАГ 1: Определяем измененные файлы (включая новые и удаленные)
echo "=== ШАГ 1: Измененные файлы ==="
CHANGED_FILES=$(git diff --name-only HEAD)
GIT_STATUS_OUTPUT=$(git status --short)
GIT_STATUS_FILTERED=$(mktemp)
echo "$GIT_STATUS_OUTPUT" | grep "^??" > "$GIT_STATUS_FILTERED" 2>/dev/null || true
# КРИТИЧНО: Извлекаем новые файлы и директории отдельно
NEW_FILES=""
NEW_DIRS=""
while IFS= read -r line; do
    [ ! -z "$line" ] || continue
    file_path=$(echo "$line" | awk '{print $2}')
    [ ! -z "$file_path" ] || continue
    if [ -d "$file_path" ]; then
        # Если это директория - добавляем в NEW_DIRS
        NEW_DIRS="$NEW_DIRS $file_path"
    elif [ -f "$file_path" ]; then
        # Если это файл - добавляем в NEW_FILES
        NEW_FILES="$NEW_FILES $file_path"
    fi
done < "$GIT_STATUS_FILTERED"
rm -f "$GIT_STATUS_FILTERED"
DELETED_FILES=$(git diff --name-only --diff-filter=D HEAD)
echo "Измененные файлы: $CHANGED_FILES"
echo "Новые файлы: $NEW_FILES"
echo "Новые директории: $NEW_DIRS"
echo "Удаленные файлы: $DELETED_FILES"
# КРИТИЧНО: Проверка успешности шага 1
if [ $? -ne 0 ]; then
    stop_on_error "ШАГ 1 не выполнен успешно"
fi
# КРИТИЧНО: Проверка наличия изменений
if [ -z "$CHANGED_FILES" ] && [ -z "$NEW_FILES" ] && [ -z "$NEW_DIRS" ] && [ -z "$DELETED_FILES" ]; then
    stop_on_error "Нет изменений для коммита. Проверьте статус репозитория."
fi
# КРИТИЧНО: Сохраняем переменные сразу после определения
save_vars_to_file
echo "✓ Переменные шага 1 сохранены в .commit_vars.sh"

# ШАГ 2: Проверяем реальные изменения через git diff (НЕ про версии, а про код)
# ВАЖНО: Это внутренняя проверка, пользователь НЕ видит вывод
REAL_CHANGES_TEMP=$(mktemp)
HAS_REAL_CHANGES=0
echo "$CHANGED_FILES" | tr ' ' '\n' | while read file; do
    [ ! -z "$file" ] || continue
    DIFF_TEMP=$(mktemp)
    DIFF_TEMP2=$(mktemp)
    DIFF_TEMP3=$(mktemp)
    git diff HEAD "$file" > "$DIFF_TEMP" 2>/dev/null
    grep -E "^\+|^\-" "$DIFF_TEMP" > "$DIFF_TEMP2" 2>/dev/null || true
    grep -v -E "^\+\+\+|^\-\-\-" "$DIFF_TEMP2" > "$DIFF_TEMP" 2>/dev/null || true
    grep -v -E "Версия|Version|дата|Date" "$DIFF_TEMP" > "$DIFF_TEMP3" 2>/dev/null || true
    [ -s "$DIFF_TEMP3" ] && echo "1" >> "$REAL_CHANGES_TEMP" || true
    rm -f "$DIFF_TEMP" "$DIFF_TEMP2" "$DIFF_TEMP3"
done
[ -f "$REAL_CHANGES_TEMP" ] && [ -s "$REAL_CHANGES_TEMP" ] && HAS_REAL_CHANGES=1 || true
rm -f "$REAL_CHANGES_TEMP"
[ $HAS_REAL_CHANGES -eq 0 ] && stop_on_error "Нет реальных изменений кода, только версии/даты" || true
# КРИТИЧНО: Проверка успешности шага 2
if [ $? -ne 0 ]; then
    stop_on_error "ШАГ 2 не выполнен успешно"
fi

# ШАГ 3: Определяем текущую версию в файлах
echo "=== ШАГ 3: Определение текущей версии ==="
# КРИТИЧНО: Проверка директории проекта
PROJECT_DIR="/Volumes/FSA-PRJ/Project/FSA-AstraInstall"
[ "$(basename $(pwd))" != "FSA-AstraInstall" ] && cd "$PROJECT_DIR" || true
[ "$(basename $(pwd))" != "FSA-AstraInstall" ] && stop_on_error "Не удалось перейти в директорию проекта" || true
echo "✓ Рабочая директория: $(pwd)"

VERSION_TEMP=$(mktemp)
# КРИТИЧНО: Используем git ls-files вместо find - это исключает .gitignore и гарантирует работу только в проекте
git ls-files | grep -E '\.(py|sh|md)$' | while read f; do
  [ -f "$f" ] && head -20 "$f" | grep "V[0-9]\+\.[0-9]\+\.[0-9]\+" >> "$VERSION_TEMP" 2>/dev/null || true
done
ALL_VERSIONS=$(grep -o "V[0-9]\+\.[0-9]\+\.[0-9]\+" "$VERSION_TEMP" | sort -u | tr '\n' ' ')
CURRENT_VERSION=$(echo "$ALL_VERSIONS" | awk '{print $1}')
rm -f "$VERSION_TEMP"
echo "Найдены версии в файлах: $ALL_VERSIONS"
echo "Основная версия для замены: $CURRENT_VERSION"
# КРИТИЧНО: Проверка успешности шага 3
if [ $? -ne 0 ]; then
    stop_on_error "ШАГ 3 не выполнен успешно"
fi
# КРИТИЧНО: Проверка что версии найдены
if [ -z "$CURRENT_VERSION" ]; then
    stop_on_error "Не удалось найти версию в файлах проекта"
fi
# КРИТИЧНО: Сохраняем переменные сразу после определения
save_vars_to_file
echo "✓ Переменные шага 3 сохранены в .commit_vars.sh"

# ШАГ 4: Определяем новую версию проекта
echo "=== ШАГ 4: Определение новой версии ==="
[ ! -f "version.txt" ] && stop_on_error "Файл version.txt не найден" || true
MAJOR=$(grep "^MAJOR=" version.txt | cut -d= -f2)
MINOR=$(grep "^MINOR=" version.txt | cut -d= -f2)
[ -z "$MAJOR" ] || [ -z "$MINOR" ] && stop_on_error "Не удалось прочитать MAJOR или MINOR из version.txt" || true
CURRENT_COMMIT=$(git rev-list --count HEAD)
[ -z "$CURRENT_COMMIT" ] && stop_on_error "Не удалось определить количество коммитов" || true
NEW_PATCH=$((CURRENT_COMMIT + 1))
NEW_VERSION="V${MAJOR}.${MINOR}.${NEW_PATCH}"
echo "Новая версия: $NEW_VERSION (текущая: $CURRENT_VERSION)"
# КРИТИЧНО: Проверка успешности шага 4
if [ $? -ne 0 ]; then
    stop_on_error "ШАГ 4 не выполнен успешно"
fi
# КРИТИЧНО: Проверка новой версии
if [ -z "$NEW_VERSION" ] || [ "$NEW_VERSION" = "V.." ]; then
    stop_on_error "Не удалось сформировать новую версию: $NEW_VERSION"
fi
# КРИТИЧНО: Сохраняем переменные сразу после определения
save_vars_to_file
echo "✓ Переменные шага 4 сохранены в .commit_vars.sh"

# ШАГ 5: Собираем подробную информацию об изменениях и сохраняем в файл
echo "=== ШАГ 5: Сбор подробной информации об изменениях ==="
echo "=== Сбор данных для анализа... ==="
TODAY=$(date +%Y.%m.%d)
ANALYSIS_DATA_FILE=".commit_analysis_data.txt"
rm -f "$ANALYSIS_DATA_FILE"
echo "$CHANGED_FILES" | tr ' ' '\n' | while read file; do
    [ ! -z "$file" ] || continue
    echo "=== Файл: $file ===" >> "$ANALYSIS_DATA_FILE"
    STAT_TEMP=$(mktemp)
    git diff --stat HEAD "$file" > "$STAT_TEMP" 2>/dev/null || true
    STAT_LINE=$(tail -1 "$STAT_TEMP" 2>/dev/null || echo "")
    [ ! -z "$STAT_LINE" ] && echo "Статистика: $STAT_LINE" >> "$ANALYSIS_DATA_FILE" || true
    rm -f "$STAT_TEMP"
    DIFF_TEMP=$(mktemp)
    DIFF_TEMP2=$(mktemp)
    DIFF_TEMP3=$(mktemp)
    git diff HEAD "$file" > "$DIFF_TEMP" 2>/dev/null
    grep -E "^\+|^\-" "$DIFF_TEMP" > "$DIFF_TEMP2" 2>/dev/null || true
    grep -v -E "^\+\+\+|^\-\-\-" "$DIFF_TEMP2" > "$DIFF_TEMP" 2>/dev/null || true
    grep -v -E "Версия|Version|дата|Date" "$DIFF_TEMP" > "$DIFF_TEMP3" 2>/dev/null || true
    [ -s "$DIFF_TEMP3" ] && echo "Изменения:" >> "$ANALYSIS_DATA_FILE" && head -100 "$DIFF_TEMP3" >> "$ANALYSIS_DATA_FILE" || true
    rm -f "$DIFF_TEMP" "$DIFF_TEMP2" "$DIFF_TEMP3"
    echo "" >> "$ANALYSIS_DATA_FILE"
    echo "---" >> "$ANALYSIS_DATA_FILE"
    echo "" >> "$ANALYSIS_DATA_FILE"
done
echo "=== Ассистент анализирует изменения и формирует описание коммита... ==="
echo ""
echo "=========================================="
echo "⏸ ПАУЗА: Ассистент должен остановиться здесь"
echo "=========================================="
echo ""
echo "ИНСТРУКЦИЯ ДЛЯ АССИСТЕНТА:"
echo "1. Прочитайте данные из файла .commit_analysis_data.txt"
echo "2. Проанализируйте все изменения в файлах"
echo "3. Сформируйте краткое понятное описание на русском языке"
echo "4. Сохраните описание в файл commit_message.txt через sed/echo (НЕ использовать write tool)"
echo "5. Удалите временный файл .commit_analysis_data.txt после чтения"
echo "6. ТОЛЬКО после сохранения файла продолжайте выполнение шага 8"
echo ""
echo "❌ ЗАПРЕЩЕНО: Продолжать выполнение без создания файла commit_message.txt!"
echo ""
echo "=========================================="
# КРИТИЧНО: Проверка успешности шага 5
if [ $? -ne 0 ]; then
    stop_on_error "ШАГ 5 не выполнен успешно"
fi

# ШАГ 6-7: Анализ изменений ассистентом и сохранение описания
# КРИТИЧНО: После выполнения ШАГ 5 (когда команда завершилась и выведены маркеры паузы) ассистент должен:
# 1. ОСТАНОВИТЬСЯ и прочитать информацию из файла .commit_analysis_data.txt
# 2. Проанализировать все изменения в файлах
# 3. Сформировать краткое понятное описание на русском языке в формате коммита
# 4. КРИТИЧНО: НЕ комментировать изменения дат и версий в файлах - эти изменения делаются автоматически алгоритмом
# 5. В описании указывать ТОЛЬКО реальные изменения кода, логики, правил, структуры и т.д.
# 6. Сохранить описание в файл commit_message.txt через sed/echo (НЕ использовать write tool)
# 7. Удалить временный файл .commit_analysis_data.txt после чтения
# 8. ТОЛЬКО после сохранения файла продолжать выполнение следующей команды (ШАГ 8)
# Пользователь НЕ видит процесс анализа
#
# Формат описания должен быть:
# [Заголовок коммита - опишите кратко основные изменения]
#
# Кратко, что изменили (списком):
#
# Имя_файла1:
# - Краткое описание изменения 1
# - Краткое описание изменения 2
#
# Имя_файла2:
# - Краткое описание изменения 1
#
# Описание удаленных классов/модулей:
# - Нет удаленных компонентов (или описание если есть)
#
# Проект: FSA-AstraInstall
# Дата: $TODAY (использовать переменную TODAY, определенную через $(date +%Y.%m.%d))
#
# ПРИМЕР команды для сохранения (ассистент должен выполнить после анализа):
# TODAY=$(date +%Y.%m.%d)
# echo "[Заголовок коммита]" > commit_message.txt
# echo "" >> commit_message.txt
# echo "Кратко, что изменили (списком):" >> commit_message.txt
# echo "" >> commit_message.txt
# echo "ASSISTANT_RULES.md:" >> commit_message.txt
# echo "- Изменен алгоритм создания снимка: добавлены шаги 3-6 для предварительного анализа" >> commit_message.txt
# ... и т.д.
# echo "Проект: FSA-AstraInstall" >> commit_message.txt
# echo "Дата: $TODAY" >> commit_message.txt
#
# После сохранения ассистент должен сообщить что описание готово и можно продолжать

# ШАГ 8: Проверяем файл сообщения коммита
echo "=== ШАГ 8: Проверка файла сообщения коммита ==="
[ ! -f "commit_message.txt" ] || [ ! -s "commit_message.txt" ] && stop_on_error "Файл commit_message.txt не найден или пуст. Ассистент должен был сформировать описание в ШАГ 6-7." || true
echo "✓ Файл commit_message.txt готов для коммита"
# КРИТИЧНО: Проверка успешности шага 8
if [ $? -ne 0 ]; then
    stop_on_error "ШАГ 8 не выполнен успешно"
fi

# ШАГ 8.5: Ожидание разрешения на продолжение (возможность редактирования файла)
echo "=== ШАГ 8.5: Ожидание разрешения на продолжение ==="
echo ""
echo "=== Описание коммита готово ==="
echo ""
echo "Содержимое файла commit_message.txt:"
echo "----------------------------------------"
cat commit_message.txt
echo "----------------------------------------"
echo ""
echo "=== Проверка переменных для продолжения ==="
VARS_FILE=".commit_vars.sh"
# КРИТИЧНО: Проверяем, что файл существует и содержит переменные
if [ ! -f "$VARS_FILE" ] || [ ! -s "$VARS_FILE" ]; then
    stop_on_error "Файл $VARS_FILE не был создан или пуст. Переменные должны были быть сохранены в шагах 1, 3, 4."
fi
# Обновляем файл с актуальными значениями (на случай если что-то изменилось)
save_vars_to_file
echo "✓ Переменные проверены и обновлены в $VARS_FILE"
echo ""
echo "=========================================="
echo "🚨 КРИТИЧНО: ОБЯЗАТЕЛЬНАЯ ПАУЗА ДЛЯ АССИСТЕНТА 🚨"
echo "=========================================="
echo ""
echo "⏸ АССИСТЕНТ ДОЛЖЕН ОСТАНОВИТЬСЯ ЗДЕСЬ И НЕ ПРОДОЛЖАТЬ АВТОМАТИЧЕСКИ!"
echo ""
echo "ИНСТРУКЦИЯ ДЛЯ АССИСТЕНТА:"
echo "1. Покажите пользователю содержимое файла commit_message.txt (выведено выше)"
echo "2. Спросите пользователя: 'Файл commit_message.txt готов. Нужно что-то изменить?'"
echo "3. ДОЖДИТЕСЬ ответа пользователя через чат (пользователь отвечает прямо в этом чате)"
echo "4. Если пользователь просит изменить - отредактируйте файл через search_replace или write, затем снова покажите содержимое и спросите"
echo "5. Если пользователь отвечает 'продолжай', '+', 'да', 'ок' или любое подтверждение - это разрешение на продолжение"
echo "6. ТОЛЬКО после получения ответа пользователя в чате продолжайте выполнение следующих команд (шаг 9)"
echo ""
echo "⚠️ ВАЖНО: Перед продолжением (шаг 9) ассистент ДОЛЖЕН загрузить переменные из файла .commit_vars.sh:"
echo "   source .commit_vars.sh"
echo "   или"
echo "   . .commit_vars.sh"
echo ""
echo "❌ ЗАПРЕЩЕНО: Продолжать выполнение автоматически без ответа пользователя!"
echo "❌ ЗАПРЕЩЕНО: Выполнять следующие команды (шаг 9 и далее) без явного разрешения!"
echo "❌ ЗАПРЕЩЕНО: Продолжать без загрузки переменных из .commit_vars.sh!"
echo ""
echo "=========================================="
echo "⏸ ПАУЗА: Ожидание разрешения пользователя"
echo "=========================================="
# КРИТИЧНО: Проверка успешности шага 8.5
if [ $? -ne 0 ]; then
    stop_on_error "ШАГ 8.5 не выполнен успешно"
fi

# ШАГ 9: Обновляем дату релиза ТОЛЬКО в измененных файлах
echo "=== ШАГ 9: Загрузка переменных и проверка файла ==="
[ ! -f ".commit_vars.sh" ] && stop_on_error "Файл .commit_vars.sh не найден. Переменные не были сохранены перед паузой." || true
source .commit_vars.sh || stop_on_error "Не удалось загрузить переменные из .commit_vars.sh"
echo "✓ Переменные загружены:"
echo "  CHANGED_FILES: $CHANGED_FILES"
echo "  CURRENT_VERSION: $CURRENT_VERSION"
echo "  NEW_VERSION: $NEW_VERSION"
[ ! -f "commit_message.txt" ] || [ ! -s "commit_message.txt" ] && stop_on_error "Файл commit_message.txt не найден или пуст. Пожалуйста, убедитесь что файл содержит сообщение коммита." || true
echo "✓ Файл commit_message.txt готов к использованию"
TODAY=$(date +%Y.%m.%d)
echo "=== ШАГ 9: Обновление дат релиза ==="
[ "$(basename $(pwd))" != "FSA-AstraInstall" ] && cd /Volumes/FSA-PRJ/Project/FSA-AstraInstall || true
echo "$CHANGED_FILES" | tr ' ' '\n' | while read file; do
    [ ! -z "$file" ] || continue
    if [[ "$file" =~ \.(py|sh|md)$ ]] && [ -f "$file" ]; then
        echo "Обновляем дату релиза в: $file"
        sed -i '' "1,20s/([0-9]\{4\}\.[0-9]\{2\}\.[0-9]\{2\})/($TODAY)/g" "$file" 2>/dev/null || true
        sed -i '' "1,20s/\"V[0-9]\+\.[0-9]\+\.[0-9]\+ ([0-9]\{4\}\.[0-9]\{2\}\.[0-9]\{2\})\"/\"V[0-9]\+\.[0-9]\+\.[0-9]\+ ($TODAY)\"/g" "$file" 2>/dev/null || true
    fi
done
# КРИТИЧНО: Проверка успешности шага 9
if [ $? -ne 0 ]; then
    stop_on_error "ШАГ 9 не выполнен успешно"
fi

# ШАГ 10: Проверяем обновление дат релиза
echo "=== ШАГ 10: Проверка обновления дат релиза ==="
TODAY=$(date +%Y.%m.%d)
ERROR_FILE=$(mktemp)
echo "$CHANGED_FILES" | tr ' ' '\n' | while read file; do
    [ ! -z "$file" ] || continue
    if [[ "$file" =~ \.(py|sh|md)$ ]]; then
        echo "Проверка $file:"
        if head -20 "$file" | grep -qF "($TODAY)"; then
            echo "✓ Дата обновлена в $file"
            head -20 "$file" | grep -F "($TODAY)" | head -3
        else
            echo "✗ ОШИБКА: Дата не обновлена в $file (ожидалось: $TODAY)"
            echo "ERROR" >> "$ERROR_FILE"
        fi
    fi
done
[ -f "$ERROR_FILE" ] && [ -s "$ERROR_FILE" ] && ERROR_COUNT=$(wc -l < "$ERROR_FILE") && rm "$ERROR_FILE" && stop_on_error "Обнаружено $ERROR_COUNT файл(ов), в которых дата релиза не была обновлена! Ожидалось: $TODAY" || rm -f "$ERROR_FILE"
echo "✓ Все даты релиза успешно обновлены"
# КРИТИЧНО: Проверка успешности шага 10
if [ $? -ne 0 ]; then
    stop_on_error "ШАГ 10 не выполнен успешно"
fi

# ШАГ 11: Обновляем версию проекта во ВСЕХ файлах
echo "=== ШАГ 11: Обновление версий проекта ==="
# КРИТИЧНО: Проверка директории проекта
PROJECT_DIR="/Volumes/FSA-PRJ/Project/FSA-AstraInstall"
[ "$(basename $(pwd))" != "FSA-AstraInstall" ] && cd "$PROJECT_DIR" || true
[ "$(basename $(pwd))" != "FSA-AstraInstall" ] && stop_on_error "Не удалось перейти в директорию проекта" || true
echo "✓ Рабочая директория: $(pwd)"

# КРИТИЧНО: Загружаем переменные из .commit_vars.sh
[ -f ".commit_vars.sh" ] && source .commit_vars.sh || stop_on_error "Файл .commit_vars.sh не найден. Невозможно продолжить без переменных."
[ -z "$NEW_VERSION" ] && stop_on_error "Переменная NEW_VERSION не загружена" || true
[ ! -f "version.txt" ] && stop_on_error "Файл version.txt не найден" || true
MAJOR=$(grep "^MAJOR=" version.txt | cut -d= -f2)
MINOR=$(grep "^MINOR=" version.txt | cut -d= -f2)
[ -z "$MAJOR" ] || [ -z "$MINOR" ] && stop_on_error "Не удалось прочитать MAJOR или MINOR из version.txt" || true

echo "Обновление версий на: $NEW_VERSION"
# КРИТИЧНО: Используем git ls-files вместо find
# Обрабатываем каждый файл отдельно с выводом прогресса
FILE_COUNT=$(git ls-files | grep -E '\.(py|sh|md)$' | wc -l | xargs)
CURRENT_FILE=0

git ls-files | grep -E '\.(py|sh|md)$' | while read f; do
  [ ! -f "$f" ] && continue
  CURRENT_FILE=$((CURRENT_FILE + 1))
  echo "[$CURRENT_FILE/$FILE_COUNT] Обработка: $f"
  
  # Заменяем ЛЮБУЮ версию формата VX.Y.Z на NEW_VERSION
  # КРИТИЧНО: В macOS sed не поддерживает \+, используем [0-9][0-9]*
  # КРИТИЧНО: Используем паттерн напрямую в команде sed для избежания проблем с подстановкой переменных
  # КРИТИЧНО: Проверяем, что NEW_VERSION не пуста перед выполнением замены
  [ -z "$NEW_VERSION" ] && echo "  ✗ ОШИБКА: NEW_VERSION пуста, пропускаем $f" && continue
  sed -i '' "1,20s/V[0-9][0-9]*\\.[0-9][0-9]*\\.[0-9][0-9]*/${NEW_VERSION}/g" "$f" 2>/dev/null || true
  
  echo "  ✓ Обработан: $f"
done
echo "✓ Версии обновлены во всех файлах"
# КРИТИЧНО: Проверка успешности шага 11
if [ $? -ne 0 ]; then
    stop_on_error "ШАГ 11 не выполнен успешно"
fi

# ШАГ 12: Проверяем обновление версий
echo "=== ШАГ 12: Проверка обновления версий ==="
# КРИТИЧНО: Проверка директории проекта
PROJECT_DIR="/Volumes/FSA-PRJ/Project/FSA-AstraInstall"
[ "$(basename $(pwd))" != "FSA-AstraInstall" ] && cd "$PROJECT_DIR" || true
[ "$(basename $(pwd))" != "FSA-AstraInstall" ] && stop_on_error "Не удалось перейти в директорию проекта" || true

[ -z "$MAJOR" ] || [ -z "$MINOR" ] && \
    [ ! -f "version.txt" ] && stop_on_error "Файл version.txt не найден" || true && \
    MAJOR=$(grep "^MAJOR=" version.txt | cut -d= -f2) && \
    MINOR=$(grep "^MINOR=" version.txt | cut -d= -f2) && \
    [ -z "$MAJOR" ] || [ -z "$MINOR" ] && stop_on_error "Не удалось прочитать MAJOR или MINOR из version.txt" || true || true

VERSION_ERRORS=0
VERSION_ERROR_FILES=""
VERSION_ERROR_TEMP=$(mktemp)
# КРИТИЧНО: Используем git ls-files вместо find
git ls-files | grep -E '\.(py|sh|md)$' | while read f; do
  [ -f "$f" ] && \
    if head -20 "$f" | grep -q "$NEW_VERSION"; then
        echo "✓ Версия обновлена в $f"
    else
        # Проверяем наличие любой версии формата VX.Y.Z
        # КРИТИЧНО: В macOS grep/sed не поддерживают \+, используем [0-9][0-9]*
        if head -20 "$f" | grep -q "V[0-9][0-9]*\\.[0-9][0-9]*\\.[0-9][0-9]*"; then
            OLD_VER=$(head -20 "$f" | grep -o "V[0-9][0-9]*\\.[0-9][0-9]*\\.[0-9][0-9]*" | head -1)
            echo "✗ ОШИБКА: Версия НЕ обновлена в $f (найдена: $OLD_VER, ожидалось: $NEW_VERSION)"
            echo "$f" >> "$VERSION_ERROR_TEMP"
        else
            echo "ℹ Файл $f не содержит версию в начале файла (пропускаем)"
        fi || true
    fi || true
done
VERSION_ERRORS=$(wc -l < "$VERSION_ERROR_TEMP" 2>/dev/null | xargs || echo "0")
# xargs удаляет пробелы и переводы строк для безопасности
VERSION_ERROR_FILES=$(cat "$VERSION_ERROR_TEMP" 2>/dev/null | tr '\n' ' ' || echo "")
rm -f "$VERSION_ERROR_TEMP"
# Явная проверка с обработкой ошибок
if [ "${VERSION_ERRORS:-0}" -gt 0 ] 2>/dev/null; then
    stop_on_error "Версия не обновлена в $VERSION_ERRORS файл(ах):$VERSION_ERROR_FILES"
fi
echo "✓ Все версии успешно обновлены"
# КРИТИЧНО: Проверка успешности шага 12
if [ $? -ne 0 ]; then
    stop_on_error "ШАГ 12 не выполнен успешно"
fi

# ШАГ 13: Добавляем измененные файлы явно (НЕ используем git add .)
echo "=== ШАГ 13: Добавление файлов в индекс ==="
# КРИТИЧНО: Проверка директории проекта
PROJECT_DIR="/Volumes/FSA-PRJ/Project/FSA-AstraInstall"
[ "$(basename $(pwd))" != "FSA-AstraInstall" ] && cd "$PROJECT_DIR" || true
[ "$(basename $(pwd))" != "FSA-AstraInstall" ] && stop_on_error "Не удалось перейти в директорию проекта" || true

ADD_ERROR_FILE=$(mktemp)
echo "$CHANGED_FILES" | tr ' ' '\n' | while read file; do
    [ ! -z "$file" ] || continue
    git add "$file" 2>/dev/null && echo "✓ Добавлен: $file" || (echo "✗ ОШИБКА: Не удалось добавить $file в индекс" && echo "ERROR" >> "$ADD_ERROR_FILE")
done
# Добавляем новые файлы
echo "$NEW_FILES" | tr ' ' '\n' | while read file; do
    [ ! -z "$file" ] || continue
    [ -f "$file" ] && \
        (git add "$file" 2>/dev/null && echo "✓ Добавлен новый: $file" || (echo "✗ ОШИБКА: Не удалось добавить новый файл $file в индекс" && echo "ERROR" >> "$ADD_ERROR_FILE")) || \
        (echo "✗ ОШИБКА: Новый файл $file не существует" && echo "ERROR" >> "$ADD_ERROR_FILE")
done
# Добавляем новые директории (рекурсивно)
echo "$NEW_DIRS" | tr ' ' '\n' | while read dir; do
    [ ! -z "$dir" ] || continue
    [ -d "$dir" ] && \
        (git add "$dir" 2>/dev/null && echo "✓ Добавлена директория: $dir" || (echo "✗ ОШИБКА: Не удалось добавить директорию $dir в индекс" && echo "ERROR" >> "$ADD_ERROR_FILE")) || \
        (echo "✗ ОШИБКА: Новая директория $dir не существует" && echo "ERROR" >> "$ADD_ERROR_FILE")
done
echo "$DELETED_FILES" | tr ' ' '\n' | while read file; do
    [ ! -z "$file" ] || continue
    git add "$file" 2>/dev/null && echo "✓ Добавлен удаленный: $file" || (echo "✗ ОШИБКА: Не удалось добавить удаленный файл $file в индекс" && echo "ERROR" >> "$ADD_ERROR_FILE")
done
# КРИТИЧНО: Используем git ls-files вместо find для поиска измененных файлов с версиями
git ls-files | grep -E '\.(py|sh|md)$' | while read f; do
    [ -f "$f" ] && \
    ! git diff --quiet HEAD -- "$f" 2>/dev/null && \
        (git add "$f" 2>/dev/null && echo "✓ Добавлен измененный: $f" || (echo "✗ ОШИБКА: Не удалось добавить измененный файл $f в индекс" && echo "ERROR" >> "$ADD_ERROR_FILE")) || true
done
[ -f "$ADD_ERROR_FILE" ] && [ -s "$ADD_ERROR_FILE" ] && ADD_ERROR_COUNT=$(wc -l < "$ADD_ERROR_FILE") && rm "$ADD_ERROR_FILE" && stop_on_error "Не удалось добавить $ADD_ERROR_COUNT файл(ов) в индекс. Проверьте права доступа и состояние файлов." || rm -f "$ADD_ERROR_FILE"
echo "=== Итоговый статус индекса ==="
git status --short
STAGED_FILES=$(git diff --cached --name-only)
[ -z "$STAGED_FILES" ] && stop_on_error "Нет файлов в индексе для коммита. Файлы не были добавлены в индекс." || true
# КРИТИЧНО: Проверка успешности шага 13
if [ $? -ne 0 ]; then
    stop_on_error "ШАГ 13 не выполнен успешно"
fi

# ШАГ 14: Создаем коммит
echo "=== ШАГ 14: Создание коммита ==="
git commit -F commit_message.txt || stop_on_error "Не удалось создать коммит. Проверьте сообщение коммита и состояние репозитория."
# КРИТИЧНО: Проверка успешности шага 14
if [ $? -ne 0 ]; then
    stop_on_error "ШАГ 14 не выполнен успешно"
fi

# ШАГ 15: Верификация коммита
echo "=== ШАГ 15: Верификация коммита ==="
git log -1 --stat > /dev/null 2>&1 || stop_on_error "Не удалось получить информацию о последнем коммите. Коммит не был создан."
git log -1 --stat
COMMIT_HASH=$(git log -1 --format="%H")
[ -z "$COMMIT_HASH" ] && stop_on_error "Коммит не найден. Коммит не был создан." || true
echo "✓ Коммит успешно создан: $COMMIT_HASH"
# КРИТИЧНО: Проверка успешности шага 15
if [ $? -ne 0 ]; then
    stop_on_error "ШАГ 15 не выполнен успешно"
fi

# ШАГ 16: Удаляем временные файлы
echo "=== ШАГ 16: Удаление временных файлов ==="
rm -f commit_message.txt .commit_vars.sh .commit_analysis_data.txt
echo "✓ Временные файлы удалены"
# КРИТИЧНО: Проверка успешности шага 16
if [ $? -ne 0 ]; then
    stop_on_error "ШАГ 16 не выполнен успешно"
fi

# ШАГ 17: Проверяем, нужна ли локальная история (изменён ли один из ключевых файлов)
echo "=== ШАГ 17: Проверка необходимости истории ==="
CHANGED_FILES_FROM_GIT=$(git log -1 --name-only --format="")
NEED_HISTORY=no
echo "$CHANGED_FILES_FROM_GIT" | grep -qE "(FSA-AstraInstall\.py|FSA-AstraInstall-1-7|FSA-AstraInstall-1-8)" && NEED_HISTORY=yes || true
echo "NEED_HISTORY=$NEED_HISTORY"
# КРИТИЧНО: Проверка успешности шага 17
if [ $? -ne 0 ]; then
    stop_on_error "ШАГ 17 не выполнен успешно"
fi

# ШАГ 18: Создаем локальную историю (только если NEED_HISTORY=yes)
[ "$NEED_HISTORY" = "yes" ] && \
  echo "=== ШАГ 18: Создание локальной истории ===" && \
  COMMIT_DATE=$(date +%Y_%m_%d_%H_%M) && \
  mkdir -p "History/$COMMIT_DATE" && \
  cp FSA-AstraInstall.py "History/$COMMIT_DATE/" 2>/dev/null || true && \
  cp FSA-AstraInstall-1-7 "History/$COMMIT_DATE/" 2>/dev/null || true && \
  cp FSA-AstraInstall-1-8 "History/$COMMIT_DATE/" 2>/dev/null || true && \
  echo "=== ШАГ 19: Валидация истории ===" && \
  ls -la "History/$COMMIT_DATE/" || \
  echo "История не требуется (ключевые файлы не изменялись)"
# КРИТИЧНО: Проверка успешности шага 18
if [ $? -ne 0 ]; then
    stop_on_error "ШАГ 18 не выполнен успешно"
fi

# ШАГ 20: Подтверждение завершения и отчёт
echo "=== ШАГ 20: Подтверждение завершения и отчёт ==="
echo "=== Снимок (коммит) успешно создан! ==="
echo ""
echo "=== Отчёт о проделанной работе ==="
echo ""
echo "Хеш коммита:"
git log -1 --format="%H"
echo ""
echo "Краткое сообщение коммита:"
git log -1 --format="%s"
echo ""
echo "Изменённые файлы:"
git diff --name-only HEAD~1 HEAD
echo ""
echo "Статистика изменений:"
git diff --stat HEAD~1 HEAD
echo ""
[ "$NEED_HISTORY" = "yes" ] && \
    echo "Локальная история создана:" && \
    echo "History/$COMMIT_DATE/" && \
    echo "" || true
echo "=== Процесс завершён успешно ==="
# КРИТИЧНО: Проверка успешности шага 20
if [ $? -ne 0 ]; then
    stop_on_error "ШАГ 20 не выполнен успешно"
fi
```