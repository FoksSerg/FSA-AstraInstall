# План реализации системы двойных потоков логирования
## Dual Stream Logging Architecture Implementation Plan

**Дата начала:** 29 октября 2025  
**Проект:** FSA-AstraInstall  
**Ветка разработки:** master  
**Резервная ветка:** simple-package-monitoring

---

## Общая концепция

### Цель
Разделить логирование на два независимых потока:
1. **RAW-поток** — необработанный вывод apt-get и системных процессов (без задержек)
2. **ANALYSIS-поток** — аналитика, парсинг, мониторинг нашей системы

### Ключевые преимущества
- ✅ Нулевая задержка для apt-get (критично для производительности)
- ✅ Независимое логирование в разные файлы
- ✅ Переключение между потоками в GUI для диагностики
- ✅ Возможность replay логов для отладки парсинга на macOS без реального apt
- ✅ Чистая архитектура с разделением ответственности

### Ограничения
- ❌ Цветные маркеры НЕ используем (совместимость со старыми сборками Linux)
- ⚠️ Минимальные изменения в существующем коде (обратная совместимость)
- ⚠️ Thread-safety обязателен (многопоточная работа)

### Важные архитектурные принципы
- ✅ **custom_print остаётся неизменным** — универсальный обработчик продолжает работать как есть
- ✅ **Изменяем только маршрутизацию apt** — stdout/stderr процессов идёт в RAW-поток
- ✅ **Папка Log/ уже создана** — скрипт запуска гарантирует её существование
- ✅ **Основной лог существует** — путь в глобальной переменной, убираем из него только apt
- ✅ **Ротация только по размеру** — при превышении разумного лимита файла

---

## ФАЗА 1: Базовая инфраструктура DualStreamLogger

### Шаг 1.1: Создание базового класса DualStreamLogger
**Файл:** `src/dual_stream_logger.py`

- [ ] Создать класс `DualStreamLogger` с инициализацией
- [ ] Реализовать два буфера в памяти (`collections.deque` с maxlen)
  - `_raw_buffer` — для вывода процессов
  - `_analysis_buffer` — для аналитики
- [ ] Добавить потокобезопасность (`threading.Lock` для каждого буфера)
- [ ] Реализовать методы записи:
  - `write_raw(message: str, timestamp: bool = True)`
  - `write_analysis(message: str, timestamp: bool = True)`
- [ ] Реализовать методы чтения:
  - `get_raw_buffer() -> List[str]`
  - `get_analysis_buffer() -> List[str]`
  - `get_raw_last_n(n: int) -> List[str]`
  - `get_analysis_last_n(n: int) -> List[str]`

**Критерии готовности:**
- Класс импортируется без ошибок
- Методы записи/чтения работают корректно
- Thread-safety проверен базовым тестом

**Тест:**
```python
# Простой тест многопоточной записи
import threading
logger = DualStreamLogger()
threads = [threading.Thread(target=lambda: logger.write_raw(f"Test {i}")) for i in range(100)]
for t in threads: t.start()
for t in threads: t.join()
assert len(logger.get_raw_buffer()) == 100
```

---

### Шаг 1.2: Добавление файлового логирования
**Файл:** `src/dual_stream_logger.py` (расширение)

- [ ] Добавить параметры инициализации для путей к файлам
  - `raw_log_path: Optional[str]`
  - `analysis_log_path: Optional[str]`
- [ ] Реализовать асинхронную запись в файлы через `queue.Queue`
- [ ] Создать отдельный поток для записи в файлы (`_file_writer_thread`)
- [ ] Реализовать буферизованную запись (flush каждые 50 записей или 1 секунду)
- [ ] Добавить методы управления:
  - `start_file_logging()`
  - `stop_file_logging(flush=True)`
  - `flush_buffers()`
- [ ] Обработка исключений при записи в файлы

**Критерии готовности:**
- Запись в файлы не блокирует основные потоки
- Данные корректно сохраняются в файлы
- Graceful shutdown с flush всех буферов

**Тест:**
```python
# Тест асинхронной записи
logger = DualStreamLogger(raw_log_path="/tmp/test_raw.log")
logger.start_file_logging()
for i in range(1000):
    logger.write_raw(f"Line {i}")
logger.stop_file_logging(flush=True)
# Проверить что все 1000 строк в файле
```

---

### Шаг 1.3: Интеграция с существующей системой путей
**Файл:** `src/dual_stream_logger.py` (доработка)

- [ ] Принимать путь к существующему логу ANALYSIS из глобальной переменной
- [ ] Генерация имени только для RAW-файла по шаблону:
  - RAW: `Log/apt_raw_YYYYMMDD_HHMMSS.log`
  - ANALYSIS: использовать существующий путь из глобальной переменной
- [ ] Добавить метод `create_default_logger(raw_log_path: str, analysis_log_path: str) -> DualStreamLogger`
- [ ] Добавить ротацию только по размеру файла (при превышении разумного лимита, например 100MB)
- [ ] Документация класса (docstrings для всех публичных методов)

**Критерии готовности:**
- RAW-лог создаётся с правильным именем
- ANALYSIS использует существующий путь
- Ротация работает при превышении лимита размера
- Документация полная и понятная

**Тест:**
```python
# Тест интеграции с существующими путями
existing_analysis_log = "Log/astra_automation_macos_20251029_120000.log"
logger = DualStreamLogger.create_default_logger(
    raw_log_path="Log/apt_raw_20251029_120000.log",
    analysis_log_path=existing_analysis_log
)
logger.write_raw("apt output")
logger.write_analysis("parser message")
logger.stop_file_logging(flush=True)
# Проверить что RAW создан, ANALYSIS дописан
```

---

## ФАЗА 2: Интеграция с UniversalProcessRunner

### Шаг 2.1: Добавление DualStreamLogger в UniversalProcessRunner
**Файл:** `astra_automation.py` (класс UniversalProcessRunner)

- [ ] Добавить параметр `dual_logger: Optional[DualStreamLogger]` в конструктор
- [ ] Сохранить ссылку на logger как атрибут класса
- [ ] Добавить fallback на существующий механизм если logger=None
- [ ] Обновить все места создания `UniversalProcessRunner` для передачи logger

**Критерии готовности:**
- Код компилируется без ошибок
- Существующая функциональность не сломана
- Logger передаётся корректно

**Тест:**
```python
# Тест инициализации с logger
logger = DualStreamLogger()
runner = UniversalProcessRunner(
    command=["echo", "test"],
    dual_logger=logger
)
assert runner.dual_logger is logger
```

---

### Шаг 2.2: Перенаправление stdout/stderr процессов в RAW-поток
**Файл:** `astra_automation.py` (метод `_read_output` в UniversalProcessRunner)

- [ ] Модифицировать `_read_output()` для записи в `dual_logger.write_raw()`
- [ ] **НЕ трогать custom_print** — он продолжает работать как есть для всех других сообщений
- [ ] Только вывод apt-процессов направить в RAW-поток (stdout/stderr)
- [ ] Сохранить минимальную задержку (проверить performance)
- [ ] Добавить обработку None для dual_logger (обратная совместимость)
- [ ] Сохранить временные метки для каждой строки

**Критерии готовности:**
- Вывод apt-процессов идёт в raw-поток
- custom_print работает как и раньше
- Задержка < 1ms на запись
- Старый код работает если dual_logger=None

**Тест:**
```bash
# Запустить простую команду и проверить raw-поток
logger = DualStreamLogger()
runner = UniversalProcessRunner(command=["echo", "Hello World"], dual_logger=logger)
runner.run()
raw_output = logger.get_raw_buffer()
assert "Hello World" in "\n".join(raw_output)
```

---

### Шаг 2.3: Интеграция custom_print с DualStreamLogger (НЕ МЕНЯЯ ЕГО ЛОГИКУ)
**Файл:** `astra_automation.py` (функция custom_print)

- [ ] **custom_print остаётся неизменным** — вся его логика сохраняется
- [ ] Добавить ДОПОЛНИТЕЛЬНУЮ запись в `dual_logger.write_analysis()` (если logger передан)
- [ ] Это просто дублирование вывода в ANALYSIS-поток, основная логика не меняется
- [ ] Проверить что channels, фильтрация, форматирование работают как раньше
- [ ] Обработка None для dual_logger (не ломаем старый код)

**Критерии готовности:**
- custom_print работает абсолютно как раньше
- Дополнительно дублирует вывод в analysis-поток (если logger есть)
- Channels, фильтры, форматирование не сломаны
- Обратная совместимость 100%

**Тест:**
```python
# Тест что custom_print работает как раньше + дублирует в analysis
logger = DualStreamLogger()
# custom_print продолжает работать как раньше
custom_print("Test message")  # должно работать без изменений
# Проверить что сообщение попало и в терминал И в analysis-буфер
assert len(logger.get_analysis_buffer()) >= 1
```

---

## ФАЗА 3: GUI — переключение потоков

### Шаг 3.1: Добавление виджета переключения потоков
**Файл:** `astra_automation.py` (метод `setup_terminal_tab`)

- [ ] Создать Frame для контролов переключения
- [ ] Добавить радиокнопки: `[○ APT Output] [○ Analysis & Parsing]`
- [ ] Добавить переменную `self.active_stream` (StringVar: "raw"/"analysis")
- [ ] Подключить callback на изменение радиокнопок
- [ ] Добавить чекбокс `[Auto-scroll: ☑]`
- [ ] Добавить кнопку `[Clear Current Stream]`

**Критерии готовности:**
- Виджеты отображаются корректно
- Переключение работает (меняется значение переменной)
- UI не сломан

**Тест:**
```python
# Ручной тест: запустить GUI, проверить визуально переключение
```

---

### Шаг 3.2: Реализация переключения содержимого терминала
**Файл:** `astra_automation.py` (новый метод `switch_terminal_stream`)

- [ ] Создать метод `switch_terminal_stream(stream_type: str)`
- [ ] Очистка текущего содержимого `terminal_text_widget`
- [ ] Загрузка данных из соответствующего буфера logger
- [ ] Вставка данных в terminal_text_widget
- [ ] Прокрутка вниз если auto-scroll включен
- [ ] Подключить метод к радиокнопкам

**Критерии готовности:**
- Переключение между потоками работает мгновенно
- Содержимое корректно отображается
- Auto-scroll работает

**Тест:**
```python
# Заполнить оба потока, переключиться несколько раз
# Проверить что содержимое меняется корректно
```

---

### Шаг 3.3: Обновление отображения активного потока в реальном времени
**Файл:** `astra_automation.py` (метод `update_terminal_display`)

- [ ] Модифицировать `update_terminal_display()` для работы только с активным потоком
- [ ] Добавить проверку `self.active_stream`
- [ ] Оптимизировать: обновлять только новые строки (не перерисовывать всё)
- [ ] Добавить throttling обновлений (макс 10 обновлений/сек)
- [ ] Сохранить позицию скролла при переключении

**Критерии готовности:**
- Активный поток обновляется в реальном времени
- Неактивный поток не тормозит систему
- Performance приемлемый (< 5% CPU)

**Тест:**
```python
# Запустить длительный процесс, переключаться между потоками
# Проверить что обновления идут только для активного
```

---

### Шаг 3.4: Реализация кнопки Clear
**Файл:** `astra_automation.py` (метод `clear_current_stream`)

- [ ] Создать метод `clear_current_stream()`
- [ ] Очистка буфера в DualStreamLogger для активного потока
- [ ] Очистка отображения в terminal_text_widget
- [ ] Подтверждение пользователя (опционально)
- [ ] Подключить к кнопке Clear

**Критерии готовности:**
- Очистка работает только для активного потока
- Другой поток не затрагивается
- GUI обновляется корректно

**Тест:**
```python
# Заполнить потоки, очистить один, проверить что другой цел
```

---

## ФАЗА 4: Рефакторинг парсинга

### Шаг 4.1: Перевод UniversalPackageCounter на чтение из RAW-потока
**Файл:** `astra_automation.py` (класс UniversalPackageCounter)

- [ ] Добавить параметр `dual_logger: Optional[DualStreamLogger]` в конструктор
- [ ] Изменить источник данных: читать из `dual_logger.get_raw_buffer()`
- [ ] Реализовать инкрементальный парсинг (только новые строки)
- [ ] Добавить отслеживание последней обработанной позиции
- [ ] Убрать прямую зависимость от stdout процесса

**Критерии готовности:**
- Парсинг работает с данными из буфера
- Не обрабатываются одни и те же строки дважды
- Обратная совместимость

**Тест:**
```python
# Заполнить raw-буфер тестовыми данными apt
# Запустить парсер, проверить корректность результата
```

---

### Шаг 4.2: Запись результатов парсинга в ANALYSIS-поток
**Файл:** `astra_automation.py` (класс UniversalPackageCounter)

- [ ] Все сообщения парсера направить в `dual_logger.write_analysis()`
- [ ] Форматирование сообщений: `[PARSER] <message>`
- [ ] Логирование этапов парсинга
- [ ] Метрики производительности парсинга
- [ ] Отчёты об ошибках парсинга

**Критерии готовности:**
- Сообщения парсера видны в analysis-потоке
- RAW-поток не засоряется аналитикой
- Чёткое разделение потоков

**Тест:**
```python
# Запустить парсинг, проверить что сообщения только в analysis
```

---

### Шаг 4.3: Асинхронный парсинг (неблокирующий для apt)
**Файл:** `astra_automation.py` (класс UniversalPackageCounter)

- [ ] Вынести парсинг в отдельный поток
- [ ] Добавить очередь для обработки новых данных
- [ ] Реализовать graceful shutdown парсера
- [ ] Добавить индикатор задержки парсинга (lag detection)
- [ ] Error handling в потоке парсера

**Критерии готовности:**
- Парсинг не блокирует основной процесс
- Apt работает на полной скорости
- Задержка парсинга отслеживается

**Тест:**
```python
# Стресс-тест: высокая скорость данных, парсер не тормозит
```

---

## ФАЗА 5: Log Replay Simulator (отладка на macOS)

### Шаг 5.1: Создание класса LogReplaySimulator
**Файл:** `src/log_replay_simulator.py`

- [ ] Создать класс `LogReplaySimulator`
- [ ] Метод `load_log_file(path: str)` — загрузка лога
- [ ] Метод `replay(dual_logger: DualStreamLogger, speed: float = 1.0)` — воспроизведение
- [ ] Поддержка паузы/продолжения/остановки
- [ ] Регулировка скорости воспроизведения (0.5x - 10x)
- [ ] Парсинг временных меток для реалистичной задержки

**Критерии готовности:**
- Загрузка логов без ошибок
- Воспроизведение с правильными задержками
- Контроль воспроизведения работает

**Тест:**
```python
# Загрузить тестовый лог, воспроизвести, проверить в буфере
simulator = LogReplaySimulator()
simulator.load_log_file("test.log")
logger = DualStreamLogger()
simulator.replay(logger, speed=10.0)
assert len(logger.get_raw_buffer()) > 0
```

---

### Шаг 5.2: GUI для Log Replay
**Файл:** `astra_automation.py` (новый метод `open_replay_dialog`)

- [ ] Добавить кнопку `[🔁 Replay Log]` на вкладку Terminal
- [ ] Создать диалоговое окно выбора файла лога
- [ ] Добавить контролы: Play/Pause/Stop
- [ ] Слайдер скорости воспроизведения (0.5x - 10x)
- [ ] Прогресс-бар воспроизведения
- [ ] Интеграция с DualStreamLogger

**Критерии готовности:**
- Диалог открывается и закрывается корректно
- Файл выбирается через file dialog
- Контролы управления работают

**Тест:**
```python
# Ручной тест: открыть диалог, выбрать лог, запустить replay
```

---

### Шаг 5.3: Интеграция replay с парсером
**Файл:** `astra_automation.py` + `log_replay_simulator.py`

- [ ] Replay эмулирует реальный процесс
- [ ] Парсер обрабатывает replay как живые данные
- [ ] Возможность останавливать replay в любой момент
- [ ] Сброс состояния парсера перед replay
- [ ] Логирование результатов replay в analysis-поток

**Критерии готовности:**
- Парсер корректно работает с replay
- Результаты совпадают с оригинальным запуском
- Отладка парсинга возможна на macOS

**Тест:**
```python
# Загрузить проблемный лог с Linux
# Запустить replay на macOS
# Проверить что парсер отработал корректно
```

---

## ФАЗА 6: Финальная оптимизация и тестирование

### Шаг 6.1: Оптимизация производительности
**Файлы:** все модифицированные файлы

- [ ] Профилирование кода (cProfile)
- [ ] Оптимизация горячих точек
- [ ] Минимизация блокировок (locks)
- [ ] Оптимизация работы с GUI (батчинг обновлений)
- [ ] Тестирование на высокой нагрузке

**Критерии готовности:**
- Задержка записи в raw < 1ms
- CPU usage парсинга < 10%
- GUI responsive (нет фризов)

**Тест:**
```python
# Стресс-тест: 10000 строк/сек в течение 60 сек
# Проверить отсутствие деградации производительности
```

---

### Шаг 6.2: Интеграционное тестирование
**Тесты:** новый файл `tests/test_dual_stream_integration.py`

- [ ] Тест полного цикла: запуск процесса → парсинг → GUI
- [ ] Тест переключения потоков во время работы
- [ ] Тест replay с одновременным парсингом
- [ ] Тест записи в файлы и чтения обратно
- [ ] Тест граничных случаев (пустые логи, огромные логи, etc)

**Критерии готовности:**
- Все тесты проходят
- Покрытие кода > 80%
- Нет memory leaks

**Тест:**
```bash
# Запустить полный набор интеграционных тестов
pytest tests/test_dual_stream_integration.py -v
```

---

### Шаг 6.3: Документация и cleanup
**Файлы:** все новые и модифицированные файлы

- [ ] Обновить README.md с описанием новой архитектуры
- [ ] Создать DUAL_STREAM_ARCHITECTURE.md с техническими деталями
- [ ] Docstrings для всех новых классов и методов
- [ ] Комментарии в сложных местах кода
- [ ] Удалить debug-код и временные файлы
- [ ] Обновить .gitignore для новых логов

**Критерии готовности:**
- Документация полная и актуальная
- Код чистый и читаемый
- Нет технического долга

---

### Шаг 6.4: Тестирование на реальной системе Linux
**Среда:** реальная Astra Linux система

- [ ] Установить обновлённую версию на тестовую систему
- [ ] Запустить полный цикл установки/обновления
- [ ] Проверить работу обоих потоков
- [ ] Проверить производительность apt (не должно быть деградации)
- [ ] Собрать логи для анализа
- [ ] Экспортировать проблемные логи для replay-тестов

**Критерии готовности:**
- Все функции работают на реальной системе
- Производительность приемлемая
- Нет критических багов

**Тест:**
```bash
# Запустить на Astra Linux:
python3 astra_automation.py
# Выполнить полную установку/обновление
# Переключаться между потоками во время работы
```

---

## ФАЗА 7: Релиз и стабилизация

### Шаг 7.1: Подготовка к релизу
**Задачи:**

- [ ] Обновить версию проекта (следовать правилам версионирования)
- [ ] Создать changelog с описанием новых возможностей
- [ ] Провести финальное code review
- [ ] Проверить обратную совместимость
- [ ] Создать резервную копию текущей стабильной версии

**Критерии готовности:**
- Версия обновлена корректно
- Changelog полный
- Code review пройден

---

### Шаг 7.2: Коммит и merge
**Git операции:**

- [ ] Закоммитить все изменения с русским сообщением
- [ ] Создать тег версии
- [ ] Обновить origin/master
- [ ] Документировать процесс отката (если понадобится)

**Критерии готовности:**
- Все изменения в git
- История коммитов чистая
- Тег создан

---

### Шаг 7.3: Мониторинг и исправление багов
**Пост-релиз:**

- [ ] Мониторинг работы на production системах
- [ ] Сбор feedback от пользователей (если есть)
- [ ] Исправление найденных багов
- [ ] Оптимизация на основе реальных данных
- [ ] Планирование следующих улучшений

**Критерии готовности:**
- Критические баги исправлены
- Система стабильна
- Performance метрики в норме

---

## Зависимости между фазами

```
ФАЗА 1 (DualStreamLogger)
    ↓
ФАЗА 2 (UniversalProcessRunner integration)
    ↓
ФАЗА 3 (GUI switching) ← можно параллельно с ФАЗОЙ 4
    ↓
ФАЗА 4 (Parser refactoring)
    ↓
ФАЗА 5 (Replay simulator) ← требует ФАЗЫ 1-4
    ↓
ФАЗА 6 (Testing & optimization)
    ↓
ФАЗА 7 (Release)
```

---

## Риски и митigation

### Риск 1: Потеря производительности apt
**Митigation:** Профилирование на каждом этапе, асинхронная запись, минимизация блокировок

### Риск 2: Сломать существующую функциональность
**Митigation:** Пошаговое тестирование, обратная совместимость, резервная ветка

### Риск 3: Thread-safety проблемы
**Mitigation:** Правильное использование locks, тестирование многопоточности

### Риск 4: Memory leaks при больших логах
**Mitigation:** Ограничение размера буферов, ротация логов, мониторинг памяти

---

## Метрики успеха

- ✅ Задержка записи в RAW-поток < 1ms
- ✅ Все существующие функции работают
- ✅ Парсинг работает корректно с replay
- ✅ GUI responsive (нет зависаний)
- ✅ Код покрыт тестами > 80%
- ✅ Успешное тестирование на реальной Astra Linux

---

## Примечания

- Каждый шаг должен быть протестирован перед переходом к следующему
- При обнаружении проблем — откат на предыдущий шаг
- Регулярные коммиты после каждой завершённой фазы
- Использование резервной ветки `simple-package-monitoring` для экспериментов

---

**Статус:** 🚀 Готов к началу реализации  
**Текущая фаза:** Не начата  
**Прогресс:** 0% (0/70 шагов)

---

## История изменений плана

**29.10.2025 14:30** — Создан начальный план реализации  
**29.10.2025 15:15** — Внесены уточнения архитектуры:
- custom_print остаётся неизменным, только дублирует вывод в analysis
- Только вывод apt-процессов перенаправляется в RAW-поток
- Папка Log/ и основной лог уже существуют (создаются в скрипте запуска)
- Ротация только по размеру файла (не по времени)
- Убраны цветные маркеры для совместимости

